# v1.6.x: Settle Up Audit Table + Transactions Buckets

For strategic overview, see the [planning overview](README.md).

## Why this version

**Two related observations from the together-session ritual:**

1. The Settle Up page shows a single net number and a list of recorded payments — but if the partners disagree on the math, there's nowhere to look. Reconciling means jumping to the Transactions page, eyeballing the table, and adding up shares in your head. The page lacks an audit affordance.

2. The Transactions page header showed a flat total but no decomposition. "How much was household? How much did I spot for my partner? How much was personal?" required scrolling through the table or filtering and re-totalling each time.

Both gaps slow down the 15-minute monthly check. v1.6.0 closes them.

---

### v1.6.0: Settle Up Audit Table + Transactions Buckets
**Goal**: Surface the math behind the settlement balance, and decompose Transactions-page totals into the buckets that match the domain (household / personal / spotted / partner-paid / excluded).
**Status**: Completed (2026-05-02)
**Context**: The settlement number on Settle Up is opaque — a single figure with no breadcrumb. The Transactions page total was a single sum that flattened the household/personal/spotted distinctions the rest of the app cares about. This version adds a payer-split audit ledger to Settle Up (with copy-for-spreadsheet workflow) and bucketed header cards to Transactions, with a new `spotted` scope to make "I fronted cash for my partner" a first-class filter.
**What this unlocks**: Verifiable settlement math for the together session, faster scope decomposition on Transactions, and reusable per-payer + per-(payer × category-group) aggregates that the chat assistant could query later.

#### Backend — payer-split aggregates

- [x] **Domain: filtered split list + payer-keyed summaries**
    - Effort: M
    - What: New value objects `PayerSplitSummary` and `PayerGroupSummary` in `src/domain/reconciliation.py`. Pure functions `filter_split_transactions(...)`, `compute_payer_split_summaries(...)`, and `compute_payer_group_summaries(...)`. Exposed `ReconciliationSummary.split_transactions` so callers reuse the filtered list without re-filtering. New `UNCATEGORIZED_GROUP_NAME = "Uncategorized"` constant in `src/domain/constants.py`. Audit-row semantics: `total_share` is the payer's share of bills *they* fronted (so each row stands alone — `total_paid - total_share = partner_share`), distinct from the broader-reconciliation `PersonSummary.total_share`.
    - Files: `src/domain/reconciliation.py`, `src/domain/constants.py`, `src/domain/categories.py`

- [x] **Use case: build audit summaries inside `GetSettleUpDataUseCase`**
    - Effort: S
    - What: New private helper `_build_audit_summaries(...)` composes the two summary functions over the already-filtered split list. Result extended with `payer_splits: list[PayerSplitSummary]` and `payer_group_splits: list[PayerGroupSummary]`. No new repository calls — everything is derived from data the use case already loads.
    - Files: `src/application/use_cases/get_settle_up_data.py`

- [x] **API schemas: response shapes for the audit table**
    - Effort: S
    - What: `PayerSplitSummaryResponse` and `PayerGroupSplitSummaryResponse` (Pydantic models with `from_domain` mappers that derive `partner_share` as `total_paid - total_share`). Threaded into `SettleUpDataResponse` as `payer_splits` and `payer_group_splits`.
    - Files: `src/interface/api/schemas/settlements.py`, `src/interface/api/schemas/reconciliation.py`

- [x] **Tests: unit coverage for the new pure functions**
    - Effort: S
    - What: New `tests/unit/domain/test_payer_group_summaries.py`. Coverage of zero-share rows, sign convention (refunds), uncategorized bucket sort order, and the per-payer aggregation invariants. Existing `test_get_settle_up_data.py` extended with cases asserting `payer_splits` and `payer_group_splits` flow through the use case correctly.
    - Files: `tests/unit/domain/test_payer_group_summaries.py` (new), `tests/unit/application/test_get_settle_up_data.py`, `tests/integration/api/test_reconciliation.py`, `tests/unit/application/chat/test_tool_executor.py`

#### Frontend — Settle Up "Showing the work" audit table

- [x] **`SettleUpAuditTable` component with by-payer / by-category toggle**
    - Effort: M
    - What: New `web/src/components/SettleUpAuditTable.tsx` rendered inside the Settle Up page just below the hero card. Six-column ledger (Activity / Amount / Txns / {p0}'s share / {p1}'s share / Net), `SegmentedControl` toggle between "By payer" and "By category", per-row links into the Transactions page with the appropriate filter chips (year/month/payer/scope and category-name multi for the by-category mode). Settlement rows render in the same ledger as signed Net entries with a dashed Amount/Txns/share to make them visually distinct.
    - Files: `web/src/components/SettleUpAuditTable.tsx`, `web/src/pages/SettleUpPage.tsx`

- [x] **Copy-for-spreadsheet workflow**
    - Effort: S
    - What: A "Copy" button writes both `text/plain` (TSV with raw decimals — Sheets parses as numeric) and `text/html` (table with bold totals) clipboard payloads. Falls back to plaintext when `ClipboardItem` isn't available. Brief "Copied" confirmation for 1.5s.
    - Files: `web/src/components/SettleUpAuditTable.tsx`

- [x] **"Showing the work" — plain-English narrative + progressive disclosure**
    - Effort: S
    - What: Renamed the section from didactic "How this number works" to "Showing the work". Added a one-paragraph narrative summary that reads naturally — *"Ash fronted $2,632.09 across 33 bills; Kew fronted $6,386.72 across 33 bills. After splitting and one Venmo transfer on Apr 26, Kew owes Ash $73.40."* — derived from the same payer-split data. The full ledger is collapsed by default behind a "Show ledger" disclosure (using the existing `ExpandChevron` pattern). The sign-convention reminder ("+ in Net favors {p0}; − favors {p1}") moved into a footnote inside the disclosed area. Reuses `plural()` and `buildSettlementLabel()` from `@/lib/format` for consistent voice across the app.
    - Files: `web/src/components/SettleUpAuditTable.tsx`, `web/src/components/SettleUpAuditTable.test.tsx`

- [x] **`LinkSettlementSection` — direction fallback**
    - Effort: S
    - What: The section previously hid itself when `net_position` was null (fully settled) or when the direction had reversed (overpaid). It now uses `data.net_position ?? data.owed` as the source of truth so the link UI remains available across all balance states except "no settlement context exists for the month at all". Lets partners record additional settlements after the balance hits zero, and renders the reversed direction when a settlement overshot.
    - Files: `web/src/pages/SettleUpPage.tsx`, `web/src/pages/SettleUpPage.test.tsx`

#### Frontend — Transactions buckets + spotted scope

- [x] **`bucketTransactions` utility + new "spotted" scope**
    - Effort: M
    - What: New pure function `bucketTransactions(transactions, currentPersonId)` returns `{ total, household, householdRefunds, personal, personalSplit, spotted, partnerPaid, excluded }` — each `{ count, amount }`. Identity-aware: a transaction is "spotted" only when the current user is the payer with `payer_percentage === 0` and `household === false`. New `isInPersonalScope(...)` and `isInSpottedScope(...)` predicates. New `sumNet(...)` helper (negated so expense-heavy sets render as a positive number; `|| 0` collapses `-0`). Extended `TransactionScope` with `"spotted"` and added a `SCOPE_LABELS` map.
    - Files: `web/src/lib/transaction-filters.ts`, `web/src/lib/transaction-filters.test.ts`

- [x] **`TransactionsHeaderCards` — bucketed summary on the Transactions page**
    - Effort: M
    - What: New component renders the buckets above the table with verbal counts (`plural()`), money totals, and a chip that toggles the settlement filter. Uses the new `InfoPopover` primitive for contextual tooltips on each bucket explaining what's counted. Replaces the prior single-total header.
    - Files: `web/src/components/TransactionsHeaderCards.tsx` (new), `web/src/components/InfoPopover.tsx` (new), `web/src/pages/TransactionsPage.tsx`, `web/src/pages/TransactionsPage.test.tsx`

- [x] **Transactions table polish**
    - Effort: S
    - What: Sortable column headers with `aria-sort` and a `cycleSortState` helper. Row tint precedence formalised (saved-flash → expanded-muted → settlement-tint, with `is_excluded` suppressing the settlement tint since excluded rows already read as muted). Column header buttons gain focus-visible underline.
    - Files: `web/src/pages/TransactionsPage.tsx`

- [x] **Component primitives + format library**
    - Effort: S
    - What: `InfoPopover` extracted as a reusable primitive over `ResponsivePopover` (small `<Info>` icon trigger, popover body with `text-xs leading-relaxed`). `formatShortDate(iso)` exposed from `@/lib/format` so callers stop hand-rolling `toLocaleDateString` calls. `StatsGrid` and `TransactionFilters` minor refinements to support the new header.
    - Files: `web/src/components/InfoPopover.tsx`, `web/src/components/ResponsivePopover.tsx`, `web/src/components/StatsGrid.tsx`, `web/src/components/TransactionFilters.tsx`, `web/src/lib/format.ts`

#### Tests

- [x] **Unit + integration coverage for backend changes**
    - Effort: S
    - What: New `test_payer_group_summaries.py` covers the domain functions; `test_get_settle_up_data.py` and `test_reconciliation.py` extended for the use-case wiring. `test_tool_executor.py` updated for the schema additions.
    - Files: see backend tests above

- [x] **Frontend test coverage**
    - Effort: M
    - What: `SettleUpAuditTable.test.tsx` covers the disclosure pattern (collapsed by default, `expandLedger()` helper), six-column header, payer/category views, settlement Net direction, copy semantics, drill-through links, the empty-bills row, and a `describe("balance narrative")` block exercising the narrative across both-payers + settlement, zero-balance, singular/plural bills, waivers, and multi-settlement counts. `transaction-filters.test.ts` covers `bucketTransactions` for every bucket, `isInSpottedScope`, `isInPersonalScope`, and `sumNet`. `TransactionsPage.test.tsx` and `SettleUpPage.test.tsx` extended for the new header cards and the LinkSettlementSection direction fallback.
    - Files: `web/src/components/SettleUpAuditTable.test.tsx`, `web/src/lib/transaction-filters.test.ts`, `web/src/pages/TransactionsPage.test.tsx`, `web/src/pages/SettleUpPage.test.tsx`

---

### v1.6.1: Audit Table & Header-Card Correctness Follow-ups
**Goal**: Fix the correctness defects the financial-accuracy review (2026-07-02) found in the v1.6.0 follow-up diff and the code it touches — the header must never claim "Settled" falsely, and the audit table's Total must tie to its own rows by construction.
**Status**: Planned
**Context**: The review confirmed several defects in or adjacent to the uncommitted v1.6.0 follow-up work: a Settlement card that renders "Settled / Fully paid" whenever the settle-up query has no data (multi-month ranges, initial load), a newly added `person_summaries` API field that duplicates state exactly derivable from `payer_splits`, ledger links that target the wrong month, and float-dust rendering. Land these with (or immediately after) committing the in-flight diff.
**What this unlocks**: A header that never lies about settlement state, an audit table whose Total row is derived from the same rows it displays, and drill-through links that land where they claim.

- [ ] **Settlement card: never show "Settled / Fully paid" without data**
    - Effort: S
    - What: With a multi-month range (or while the single-month settle-up query is still loading), `useSettleUpForMonth` returns `data: null` and `isSettled = !netDirection` forces the "Settled" headline and "Fully paid" description even when the range has a large outstanding gross balance. Distinguish loading from disabled, and make the "Select a single month to see settlement balance" branch reachable — today it is provably dead (reaching it requires settle-up data, which requires a single month). Test: multi-month range with non-zero gross renders the select-a-month message, not "Settled".
    - Files: `web/src/components/TransactionsHeaderCards.tsx`, `web/src/pages/TransactionsPage.test.tsx`

- [x] **Derive the audit Total row from `payer_splits`; drop `person_summaries` from the API** *(folded into the v1.6.0 follow-up change before commit, 2026-07-02)*
    - Effort: M
    - What: `PersonSummary` totals are exactly derivable from `payer_splits` (`their_share + partner's (paid − share)`; exact to the cent because `other_share = abs − payer_share` in `src/domain/splits.py`). Removed `person_summaries` from `GetSettleUpDataResult` / `SettleUpDataResponse` / generated client; the Total row now sums the rendered ledger rows (splits + settlement Net contributions), so total and body cannot disagree in either view; deleted the 50-line `deriveServerTotals` test helper that existed only to keep the duplicate fields consistent.
    - Files: `src/application/use_cases/get_settle_up_data.py`, `src/interface/api/schemas/settlements.py`, `web/src/components/SettleUpAuditTable.tsx`, `web/src/components/SettleUpAuditTable.test.tsx`, `web/src/api/generated/**` (regenerated)

- [ ] **One sign convention for Total + narrative**
    - Effort: S
    - What: After the Total-row rework (2026-07-02), `totals` derives from the rendered rows and the component no longer reads `net_position` — but `buildBalanceNarrative` still hand-reconstructs `{amount, from, to}` from the scalar's sign (`net > 0 ? p1 : p0`). Have `computeTotals` emit the direction once (derived from the row-summed net, guarded by `isZeroCurrency`) and let both the Net cell and the narrative consume it. Do not reintroduce `net_position` here: a second source could disagree with the Total row the narrative sits beside in float-dust cases.
    - Files: `web/src/components/SettleUpAuditTable.tsx`

- [ ] **Settlement ledger rows link via the linked transactions' dates**
    - Effort: S
    - What: Row links derive year/month from local-time `new Date(s.settled_at)` — the recording moment, in UTC. A March settlement recorded during the April together session links to April, where the linked March 31 transfers don't exist; UTC→local conversion can also shift the month at boundaries. Anchor the link to the linked transactions' own dates (`linked_transactions` is already on `SettlementResponse`), falling back to `s.year`/`s.month` when no transactions are linked (waivers, manual records). Not `s.year`/`s.month` as the primary: v1.7.5 demotes a settlement's year/month to optional display annotation, and the transaction-date anchor also handles candidates from the 7-day padding window. Pre-existing bug carried through the rewrite.
    - Files: `web/src/components/SettleUpAuditTable.tsx`, `web/src/components/SettleUpAuditTable.test.tsx`

- [ ] **Uncategorized drill-through: no unfiltered URLs**
    - Effort: S
    - What: `group_id=null` rows (and every by-category row while category groups are still loading) produce a link with no category filter, so a row claiming "$142 · 3 txns" opens all of that payer's household transactions for the month. Either include the group's category names in the backend row payload, or add an uncategorized filter param to the Transactions page.
    - Files: `web/src/components/SettleUpAuditTable.tsx`, `web/src/lib/transaction-filters.ts`

- [ ] **`sumNet`: collapse float dust, not just `-0`**
    - Effort: S
    - What: `-reduce(...) || 0` only catches an exact `-0`; residue like `-5.55e-17` from summing signed amounts is truthy and Intl renders it as "-$0.00" on the In View card. Clamp through `isZeroCurrency` (its float-dust behavior is already pinned in `format.test.ts`).
    - Files: `web/src/lib/transaction-filters.ts`, `web/src/lib/transaction-filters.test.ts`

- [ ] **Review hardening (2026-07-02 /review suggestions)**
    - Effort: S
    - What: Complete `escapeHtml`'s entity set (`"` and `'`) — safe today since output only lands in element text, but a latent footgun if a future edit interpolates into an attribute. Guard the `minAmt`/`maxAmt` URL-param parsing with `Number.isFinite` so a malformed param doesn't silently filter everything out. Normalize the one hand-rolled query string in `TransactionsHeaderCards` to `URLSearchParams`. Add `scope="col"` to the ledger table headers (the only a11y gap the review found).
    - Files: `web/src/components/SettleUpAuditTable.tsx`, `web/src/lib/transaction-filters.ts`, `web/src/components/TransactionsHeaderCards.tsx`

- [ ] **DRY follow-ups from the diff**
    - Effort: S
    - What: Replace the hand-rolled "pending upload" pluralization with `plural()` (already imported and used six times in the same file). Extract the duplicated 1.5s copy-feedback machinery (`copied` state + ref-guarded timeout + cleanup effect + Check/Copy icon swap) from `SettleUpAuditTable` and chat's `CopyButton` into a shared `useCopyFeedback` hook. Expose the category lookup from `reconcile()` (or pass it in) so `_compute_audit_splits` stops rebuilding the identical dict in the same request — same reuse pattern as `split_transactions`.
    - Files: `web/src/components/TransactionsHeaderCards.tsx`, `web/src/components/SettleUpAuditTable.tsx`, `web/src/components/chat/ChatMessage.tsx`, `src/application/use_cases/get_settle_up_data.py`, `src/domain/reconciliation.py`

---

## Decisions

**Audit table sits below the hero card, not behind a tab.** Verification is the second-most-important question on Settle Up (after "what's the number?"). A tab would hide it; a separate page would force navigation away from the settlement actions. Inline-but-disclosed strikes the balance — most reads stop at the narrative, but the receipts are one click away.

**Narrative first, ledger second.** A casual reader gets the answer in plain English. A verifier expands the ledger. The progressive disclosure is the entire UX point — this isn't just a stylistic rename of "How this number works".

**Per-payer share semantics differ from `PersonSummary.total_share`.** `PayerSplitSummary.total_share` is the payer's share of bills they fronted (so `paid - share = partner_share`). `PersonSummary.total_share` is the person's share across *all* bills (used for the broader reconcile-then-settle math). Both are correct for their context — the audit table needs row-local arithmetic, the settlement math needs cross-row.

**`spotted` is a scope, not a separate type.** Internally there's no `is_spotted` flag — the predicate is `payer_percentage === 0 AND household === false AND payer_person_id === current_user`. Adding it as a `TransactionScope` value (alongside `household`, `personal`, `all`) keeps the data model orthogonal while giving users a first-class filter for the "I fronted cash for my partner" case.

**Settlement transactions are excluded from split aggregates.** `filter_split_transactions(...)` drops `is_settlement=true` and `is_excluded=true` rows along with `payer_percentage=100` (household-no-split). Settlement rows still appear in the audit table, but as recorded-settlement ledger rows, not split aggregates — they contribute signed Net values directly.

**`Uncategorized` rows sort last in by-category view.** A separate `_group_sort_key` puts `group_id is None` at the bottom; everything else sorts by `-abs(total)`. Keeps the most material categories at the top.

**Two top-level rendering modes only.** "By payer" (one row per partner) and "By category" (rows per `(payer × category-group)`). No three-way split view. Adding category as a slicing dimension covers the most common follow-up question ("which category drove most of the gap?") without a third toggle.
