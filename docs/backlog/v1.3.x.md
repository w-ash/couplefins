# v1.3.x: Per-Month Budget Model

For strategic overview, see the [planning overview](README.md).

## Scope Design Decisions

**Household vs personal budgets are completely separate views.** The scope toggle on the Budget page switches between two independent budget sets — no cross-visibility. Household scope shows household budgets only; personal scope shows personal budgets only.

**`include_personal` affects the combined budget target.** When a category has `include_personal=true`, personal spending counts against the household budget. The budget amount should be set knowing this — the average spending hint includes personal transactions (e.g., "Avg: $800 (incl. personal)") so the user sets an appropriate target.

**Copy from last month copies both scopes.** One click copies all budgets (household + personal) from the previous month, regardless of which scope the user is currently viewing.

---

### v1.3.0: Per-Month Budget Model
**Goal**: Replace the cascading effective-date budget model with simple per-month budgets. Each month has its own independent budget amounts per category group.
**Status**: Not started
**Context**: The current model stores budgets with an `effective_from` date — a budget cascades forward indefinitely until a newer one takes effect. This is correct but unintuitive. Users think "I want to budget $X for this category this month," not "I want this budget to take effect on this date and cascade." Monarch Money and YNAB both use per-month amounts. The couple reviews budgets monthly; the model should match that cadence. Deleting a budget erases history, there's no way to "stop" budgeting a category, and the effective date field had to be hidden because it confused users.
**What this unlocks**: Simpler mental model — each month is independent. Delete means delete (just this month). No effective dates. YTD is the honest sum of months where a budget was set.

#### Backend: Schema & Migration

- [ ] **Migration: `effective_from` → `year` + `month`**
    - Effort: S
    - What: Drop all rows from `category_group_budgets` (only 1 test budget exists). Drop `effective_from` column. Add `year: int` and `month: int` columns. New unique constraint on `(group_id, year, month, person_id)`.
    - Files: `alembic/versions/0006_monthly_budgets.py`

- [ ] **Domain entity update**
    - Effort: S
    - What: `CategoryGroupBudget` replaces `effective_from: date` with `year: int`, `month: int`.
    - Files: `src/domain/entities/category_group_budget.py`

- [ ] **Domain logic simplification**
    - Effort: M
    - What: Delete `resolve_effective_budget()` and `compute_ytd_budget()`. Budget lookup becomes direct match by `(group_id)` from pre-filtered list. YTD budget = sum of monthly amounts for that group across months 1..N. Simplify `_build_group_status()`, `compute_budget_overview()`, and `compute_personal_budget_overview()`.
    - Files: `src/domain/budget.py`

- [ ] **Repository updates**
    - Effort: S
    - What: `get_by_person(person_id)` → `get_by_month(year, month, person_id)`. Add `get_by_year(year, person_id)` for YTD. Keep `save`, `save_batch`, `delete`, `get_by_id`, `delete_by_group_id`.
    - Files: `src/domain/repositories/category_group_budget_repository.py`, `src/infrastructure/persistence/repositories/category_group_budget_repository.py`

- [ ] **Use case updates**
    - Effort: S
    - What: `get_budget_overview` fetches month budgets + year budgets separately. `save_budget` takes `year`/`month` instead of `effective_from`, with upsert semantics on `(group_id, year, month, person_id)`.
    - Files: `src/application/use_cases/get_budget_overview.py`, `src/application/use_cases/save_budget.py`

- [ ] **API schema updates**
    - Effort: S
    - What: `SaveBudgetRequest` and `BudgetResponse`: replace `effective_from` with `year`, `month`.
    - Files: `src/interface/api/schemas/budgets.py`, `src/interface/api/routes/budgets.py`

#### Frontend

- [ ] **Remove effective date state**
    - Effort: S
    - What: Remove `effectiveFrom` from `AddBudgetForm`. Pass `year`/`month` from viewed page. Orval codegen for new schema.
    - Files: `web/src/pages/BudgetPage.tsx`, `web/src/api/generated/**`

#### Tests

- [ ] **Rewrite budget domain tests**
    - Effort: S
    - What: Replace cascading/effective-date tests with per-month lookup tests. Test YTD = sum of monthly amounts. Test month with no budget → `monthly_budget=None`.
    - Files: `tests/unit/domain/test_budget.py`

---

### v1.3.1: Copy From Last Month + Budget Page UX
**Goal**: Bulk-copy budgets when opening a new month. Polish add/edit/delete for the per-month model.
**Status**: Not started
**Context**: With per-month budgets, each month starts empty. The most common workflow is "same as last month." Without copy, the couple re-enters every budget manually — tedious and error-prone.
**What this unlocks**: One-click budget setup for a new month. Clean delete semantics ("just this month"). Smooth monthly ritual.

#### Backend

- [ ] **Copy budgets endpoint**
    - Effort: S
    - What: `POST /budgets/copy` with `year`, `month`, `source_year`, `source_month`. Copies ALL budget records (both household and personal) from source to target month, skipping groups that already have a budget in the target month for that scope. Returns created budgets.
    - Files: `src/application/use_cases/copy_budgets.py` (new), `src/interface/api/routes/budgets.py`, `src/interface/api/schemas/budgets.py`

- [ ] **Previous month budget indicator**
    - Effort: S
    - What: Add `previous_month_has_budgets: bool` field to `BudgetOverviewResponse`.
    - Files: `src/application/use_cases/get_budget_overview.py`, `src/interface/api/schemas/budgets.py`

#### Frontend

- [ ] **Empty state with "Copy from last month"**
    - Effort: M
    - What: When current month has no budgets and previous month does, show a prominent "Copy budgets from [Month]" button. Otherwise show "Add your first budget" empty state.
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **Delete confirmation text**
    - Effort: S
    - What: Update confirm dialog to "Remove [Group] budget for [Month Year]?" — makes it clear only this month is affected.
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **"Incl. personal" hint on budget amounts**
    - Effort: S
    - What: When a group has categories with `include_personal=true`, the average spending hint in the add/edit form shows "(incl. personal)" so the user sets a target knowing it covers both household and personal spending.
    - Files: `web/src/pages/BudgetPage.tsx`

#### Tests

- [ ] **Copy budgets use case tests**
    - Effort: S
    - What: Test copy creates records, skips existing, handles empty source.
    - Files: `tests/unit/application/test_copy_budgets.py` (new)

---

### v1.3.2: Budget Trends on Insights Page
**Goal**: Fulfill US-BUDGET-5/US-BUDGET-8 (planned since v0.7.x, never shipped). Show budget limit lines overlaid on spending trend charts.
**Status**: Not started
**Context**: The Insights page has spending trend sparklines per category group, but no budget context. The couple can see spending trends but can't tell at a glance whether a group was over or under budget in a given month. With per-month budgets, each month has a concrete number to overlay.
**What this unlocks**: Data-driven budget conversations during the together session. Visual answer to "when did we start going over?"

#### Backend

- [ ] **Budget data in spending trends response**
    - Effort: S
    - What: Include per-month budget amounts in the spending trends endpoint response, keyed by group and month. Straightforward join with the new per-month budget model.
    - Files: `src/application/use_cases/get_spending_trends.py`, `src/interface/api/schemas/insights.py`

#### Frontend

- [ ] **Budget overlay lines on sparkline charts**
    - Effort: M
    - What: Dashed line showing budget amount per month on each group's spending chart. Step-line if budget varies month-to-month. Muted color, doesn't compete with spending line.
    - Files: `web/src/components/SparklineCard.tsx`, `web/src/pages/InsightsPage.tsx`

- [ ] **Month-over-month budget awareness on Budget page**
    - Effort: S
    - What: If a group's budget differs from last month, show subtle indicator (e.g., "↑ $100 from last month" or "New this month"). Helps the couple notice changes during review.
    - Files: `web/src/pages/BudgetPage.tsx`
