# v1.3.x: Per-Month Budget Model

For strategic overview, see the [planning overview](README.md).

## Scope Design Decisions

**Household vs personal budgets are completely separate views.** The scope toggle on the Budget page switches between two independent budget sets — no cross-visibility. Household scope shows household budgets only; personal scope shows personal budgets only.

**`include_personal` affects the combined budget target.** When a category has `include_personal=true`, personal spending counts against the household budget. The budget amount should be set knowing this — the average spending hint includes personal transactions (e.g., "Avg: $800 (incl. personal)") so the user sets an appropriate target.

**Copy from previous month copies household + the caller's personal budgets.** One click copies all household budgets (shared) plus the current user's personal budgets from the most recent prior month with budgets (not necessarily the immediately previous month — handles skipped months). The partner copies their own personal budgets separately on their device.

**Budget ownership.** All budget mutation endpoints (`POST`, `PUT`, `DELETE`, `POST /budgets/copy`) derive `person_id` from the authenticated user's JWT — never accepted from the request body. Household budgets use `person_id=NULL` with a partial unique index (see migration spec).

**`null` vs `0` for missing budgets.** A month with no budget record returns `null` (not `0`). `null` means "no budget set" — no overlay line on charts, $0 contribution to YTD. `0` means "explicitly budgeted at zero" — line shown at baseline. This distinction must be maintained through the full stack: domain entities, API responses, and frontend rendering.

**Deleting a budget returns the group to "no budget set."** Delete produces `null` (no record), not `0`. No overlay line on charts, no YTD contribution. This is distinct from setting a budget to $0, which is an explicit "we're spending nothing here" signal.

**`get_by_year` returns sparse results.** The repository returns only months where a budget record exists — no gap-filling, no placeholder rows. Callers sum returned amounts directly. A year with 3 budgeted months returns 3 records, not 12.

---

### v1.3.0: Per-Month Budget Model
**Goal**: Replace the cascading effective-date budget model with simple per-month budgets. Each month has its own independent budget amounts per category group.
**Status**: Completed (2026-04-04)
**Context**: The current model stores budgets with an `effective_from` date — a budget cascades forward indefinitely until a newer one takes effect. This is correct but unintuitive. Users think "I want to budget $X for this category this month," not "I want this budget to take effect on this date and cascade." Monarch Money and YNAB both use per-month amounts. The couple reviews budgets monthly; the model should match that cadence. Deleting a budget erases history, there's no way to "stop" budgeting a category, and the effective date field had to be hidden because it confused users.
**What this unlocks**: Simpler mental model — each month is independent. Delete means delete (just this month). No effective dates. YTD is the honest sum of months where a budget was set.

#### Backend: Schema & Migration

- [x] **Migration: `effective_from` → `year` + `month`**
    - Effort: S
    - What: Drop all rows from `category_group_budgets` (no production data — only 1 test budget exists). Drop `effective_from` column. Add `year: int` and `month: int` columns. Two partial unique indexes to handle nullable `person_id`: one for personal budgets (`WHERE person_id IS NOT NULL`), one for household budgets (`WHERE person_id IS NULL`).
    - Files: `alembic/versions/0006_monthly_budgets.py`

- [x] **Domain entity update**
    - Effort: S
    - What: `CategoryGroupBudget` replaces `effective_from: date` with `year: int`, `month: int`.
    - Files: `src/domain/entities/category_group_budget.py`

- [x] **Domain logic simplification**
    - Effort: M
    - What: Deleted `resolve_effective_budget()` and old `compute_ytd_budget()`. Monthly lookup via `_index_month_budgets()` (pre-built dict, O(1) per group). YTD via `_compute_ytd_budget()` — filters year_budgets by group_id and month <= N. `_build_group_status()` takes pre-indexed month budget dict. `compute_budget_overview()` and `compute_personal_budget_overview()` take separate `month_budgets`/`year_budgets` params.
    - Files: `src/domain/budget.py`

- [x] **Repository updates**
    - Effort: S
    - What: `get_by_person(person_id)` → `get_by_month(year, month, person_id)` + `get_by_year(year, person_id)`. Sparse results. Shared `_person_filter()` static method for NULL-aware person_id filtering. Kept `save`, `save_batch`, `delete`, `get_by_id`, `get_all`, `delete_by_group_id`.
    - Files: `src/domain/repositories/category_group_budget_repository.py`, `src/infrastructure/persistence/repositories/category_group_budget_repository.py`

- [x] **Use case updates**
    - Effort: S
    - What: `get_budget_overview` fetches `get_by_year` once and derives month budgets via list filter (one DB call instead of two). `save_budget` takes `year`/`month`, upserts via `get_by_month` lookup in the use case (not repo-level). `get_spending_trends` and `get_dashboard` updated. Sequential calls (not `asyncio.gather` — SQLAlchemy AsyncSession is not safe for concurrent ops on same session).
    - Files: `src/application/use_cases/get_budget_overview.py`, `src/application/use_cases/save_budget.py`, `src/application/use_cases/get_spending_trends.py`, `src/application/use_cases/get_dashboard.py`, `src/application/use_cases/list_budgets.py`

- [x] **API schema updates**
    - Effort: S
    - What: `SaveBudgetRequest` and `BudgetResponse`: replace `effective_from` with `year` (ge=2020, le=2099) and `month` (ge=1, le=12) via Pydantic `Field`. Same constraints on Command objects (validate at both boundaries). Route maps request → Command; entity construction happens in the use case.
    - Files: `src/interface/api/schemas/budgets.py`, `src/interface/api/routes/budgets.py`

- [x] **Add ownership checks to budget mutation endpoints** *(must ship atomically with v1.3.0)*
    - Effort: S
    - What: `PUT /budgets/{id}` and `DELETE /budgets/{id}` now inject `current_user`. Shared `require_budget_ownership()` helper in `_shared/budget_ownership.py`. `ForbiddenError` domain exception mapped to 403 in middleware.
    - Files: `src/interface/api/routes/budgets.py`, `src/application/use_cases/update_budget.py`, `src/application/use_cases/delete_budget.py`, `src/application/use_cases/_shared/budget_ownership.py`, `src/domain/exceptions.py`, `src/interface/api/middleware.py`

- [x] **Scope `GET /budgets` list endpoint**
    - Effort: S
    - What: `GET /budgets` injects `current_user` and filters to household + caller's personal budgets. `list_budgets()` accepts `person_id` param and filters `get_all()` results in Python (proportional to 2-user scale).
    - Files: `src/interface/api/routes/budgets.py`, `src/application/use_cases/list_budgets.py`

- [x] **Update `docs/domain.md` budget section**
    - Effort: S
    - What: Documented per-month `year`/`month` model and household (`person_id=NULL`) vs personal budget distinction.
    - Files: `docs/domain.md`

#### Frontend

- [x] **Remove effective date state**
    - Effort: S
    - What: Removed `effectiveFrom` from `AddBudgetForm` props and `onSave` callback. Form sends `year`/`month` directly from `useMonthYear()`. `CategoryMappingEditor` budget comparison updated. Orval codegen regenerated.
    - Files: `web/src/pages/BudgetPage.tsx`, `web/src/components/CategoryMappingEditor.tsx`, `web/src/api/generated/**`

#### Tests

- [x] **Update test factory**
    - Effort: S
    - What: `make_category_group_budget()`: replaced `effective_from: date` with `year: int = 2026, month: int = 1`.
    - Files: `tests/fixtures/factories.py`

- [x] **Rewrite budget domain tests**
    - Effort: M
    - What: Replaced 6 cascading/effective-date tests with per-month tests: index lookup, YTD sum, gap month, no-budget-returns-None, no-contamination regression. All overview tests updated for new `month_budgets`/`year_budgets` signatures.
    - Files: `tests/unit/domain/test_budget.py`

- [x] **Update application-layer tests (broken by schema change)**
    - Effort: M
    - What: All `effective_from` → `year`/`month`. Mock calls updated from `get_by_person()` to `get_by_year()`. Added upsert test, ownership rejection tests for update/delete, household-budget-editable-by-anyone tests.
    - Files: `tests/unit/application/test_save_budget.py`, `tests/unit/application/test_update_budget.py`, `tests/unit/application/test_delete_budget.py`, `tests/unit/application/test_get_budget_overview.py`, `tests/unit/application/test_get_spending_trends.py`

- [x] **Rewrite budget integration tests**
    - Effort: M
    - What: All 10 tests rewritten with `year`/`month`. Personal budget tests use `is_personal: true` instead of `person_id` in request body. Note: `test_upsert_budget_finalized_month_returns_409` deferred to v1.3.1 (finalization check for budgets not yet implemented).
    - Files: `tests/integration/api/test_budgets.py`

- [x] **Add budget repository integration tests**
    - Effort: S
    - What: 6 tests: `get_by_month` happy path + empty, `get_by_year` sparse + empty, upsert replaces existing, household and personal coexist for same group/month.
    - Files: `tests/integration/repositories/test_budget_repository.py` (new)

---

### v1.3.1: Copy From Previous Month + Budget Page UX
**Goal**: Bulk-copy budgets when opening a new month. Polish add/edit/delete for the per-month model.
**Status**: Not started
**Context**: With per-month budgets, each month starts empty. The most common workflow is "same as last month." Without copy, the couple re-enters every budget manually — tedious and error-prone.
**What this unlocks**: One-click budget setup for a new month. Clean delete semantics ("just this month"). Smooth monthly ritual.

#### Backend

- [ ] **Copy budgets endpoint**
    - Effort: S
    - What: `POST /budgets/copy` with `year`, `month`, `source_year`, `source_month` (same year/month validation as `SaveBudgetRequest`). Copy scope per design decisions above. Rejects with 409 if target month is finalized. Skips per `(group_id, person_id)` — household and personal checked independently. Returns created budgets.
    - Files: `src/application/use_cases/copy_budgets.py` (new), `src/interface/api/routes/budgets.py`, `src/interface/api/schemas/budgets.py`

- [ ] **Copyable source month indicator**
    - Effort: S
    - What: Add `copyable_source: {year: int, month: int} | null` field to `BudgetOverviewResponse`. Computed in the `get_budget_overview` use case (not route handler or domain). Finds the most recent prior month with budgets (per copy scope in design decisions). `null` when no prior month has budgets (first-time setup).
    - Files: `src/application/use_cases/get_budget_overview.py`, `src/interface/api/schemas/budgets.py`

- [ ] **Next month budget indicator**
    - Effort: S
    - What: Add `next_month_has_budgets: bool` field to `BudgetOverviewResponse`. Used by the "Set up next month" prompt (US-BUDGET-10) — shown when viewing a past month and the next month has no budgets yet.
    - Files: `src/application/use_cases/get_budget_overview.py`, `src/interface/api/schemas/budgets.py`

#### Frontend

- [ ] **Empty state with "Copy from [month]"**
    - Effort: M
    - What: Three-state empty view driven by `copyable_source` from the overview response: (1) **loading** — skeleton in the budget-rows/copy-card area only; month picker, scope toggle, and page chrome remain mounted and interactive (no full-page blocker), (2) **has-source** — prominent "Copy budgets from [source month name]" card, with unbudgeted group spending below as context for what to budget. (3) **no-source** — "Add your first budget" empty state, also with unbudgeted spending below. Copy button shows pending state during mutation, error toast on failure, no optimistic update. On copy success, invalidate budget overview query for `(year, month, scope)` — no success toast; the data appearing is the feedback.
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **Delete confirmation text**
    - Effort: S
    - What: Update confirm dialog to "Remove [Group] budget for [Month Year]?" — makes it clear only this month is affected.
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **"Incl. personal" hint on budget amounts**
    - Effort: S
    - What: When a group has categories with `include_personal=true`, the average spending hint in the add/edit form shows "(incl. personal)" with an accessible `?` icon button (`aria-label`, click/focus popover) explaining that personal spending counts toward this budget. Not `<abbr>` — it's not keyboard-focusable, inconsistent across screen readers, and invisible on mobile (hover-only).
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **"Set up next month" prompt**
    - Effort: S
    - What: When viewing a past month's budget and `next_month_has_budgets` is `false`, show a subtle text link near the month picker: "Set up [next month] budgets?" Month is a URL search param — clicking fires `navigate()` with updated `?year=...&month=...` so the back button works. No prompt when next month already has budgets. (US-BUDGET-10)
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **Last-month context in edit form**
    - Effort: S
    - What: When editing a budget amount inline, show the source month's amount below the input as context ("[Month]: $500" or "New this month"). Uses budget data from `copyable_source` month. Neutral color.
    - Files: `web/src/pages/BudgetPage.tsx`

#### Tests

- [ ] **Copy budgets use case tests**
    - Effort: S
    - What: Six scenarios: copy from populated source creates all records, skips groups already budgeted in target, handles empty source (creates nothing), partial target — only fills missing groups (the "no overwrites" guarantee from US-BUDGET-6), copy excludes partner's personal budgets (only household + caller's personal), copy to finalized month raises domain error.
    - Files: `tests/unit/application/test_copy_budgets.py` (new)

- [ ] **Copy budgets integration tests**
    - Effort: S
    - What: Route-level tests via AsyncClient: happy path creates records, empty source returns 200 with zero count, copy onto finalized month returns 409.
    - Files: `tests/integration/api/test_budgets.py`

- [ ] **Copyable source and next-month indicator tests**
    - Effort: S
    - What: Scenarios added to `test_get_budget_overview.py`: `copyable_source` returns the most recent prior month with budgets (not just previous month), returns `null` when no prior months have budgets, skips empty months correctly. `next_month_has_budgets` is `true`/`false` as expected.
    - Files: `tests/unit/application/test_get_budget_overview.py`

---

### v1.3.2: Budget Trends on Insights Page
**Goal**: Fulfill US-BUDGET-5/US-BUDGET-8 (planned since v0.7.x, never shipped). Show budget limit lines overlaid on spending trend charts.
**Status**: Not started
**Context**: The Insights page has spending trend sparklines per category group, but no budget context. The couple can see spending trends but can't tell at a glance whether a group was over or under budget in a given month. With per-month budgets, each month has a concrete number to overlay.
**What this unlocks**: Data-driven budget conversations during the together session. Visual answer to "when did we start going over?"

#### Backend

- [ ] **Budget data in spending trends response**
    - Effort: S
    - What: Include per-month budget amounts in the spending trends endpoint response, keyed by group and month. `null` vs `0` semantics per design decisions above. Uses `get_by_year` from the budget repository — do not reintroduce `resolve_effective_budget()`.
    - Files: `src/application/use_cases/get_spending_trends.py`, `src/interface/api/schemas/insights.py`

#### Frontend

- [ ] **Budget overlay lines on sparkline charts**
    - Effort: M
    - What: Change `SparklineCard` prop from `budgetLine?: number | null` (scalar) to `budgetAmounts?: Array<{ month: number; amount: number }>` (per-month). Render as a dashed step-line — one segment per month with a budget. Months with `null` budget create a gap in the overlay series (do not interpolate or draw to zero). Muted color, doesn't compete with spending line. **Safety note**: TypeScript will silently accept the old `budgetLine` prop at call sites (unknown props are ignored, not errors) — run `grep budgetLine web/src` before merge to catch stale references.
    - Files: `web/src/components/SparklineCard.tsx`, `web/src/pages/InsightsPage.tsx`

- [ ] **Refactor `buildBudgetMap` for per-month amounts**
    - Effort: S
    - What: `buildBudgetMap` currently produces a scalar per group from `monthly_budget`. Refactor to produce `Map<groupId, Array<{ month, amount }>>` from the new per-month budget data in the spending trends response. The backend spending trends endpoint (task above) must include per-month budget amounts keyed by group — confirm the API shape provides an array, not a single value.
    - Files: `web/src/pages/InsightsPage.tsx`

- [ ] **Month-over-month budget awareness on Budget page**
    - Effort: S
    - What: If a group's budget differs from last month, show subtle indicator (e.g., "↑ $100 from last month" or "New this month"). Use neutral color (`--color-neutral-500`) — not teal or coral, since a budget change is neither inherently good nor bad.
    - Files: `web/src/pages/BudgetPage.tsx`

- [ ] **YTD gap indicator**
    - Effort: S
    - What: In YTD view, if a budgeted group has months without a budget, show "N of M months budgeted" in the group row. Requires per-month budget presence per group across the YTD range — verify the budget overview response includes this (e.g., a `budgeted_months: int` field per group, or the full monthly breakdown). If it doesn't, add a field to the overview endpoint rather than a separate query. (US-BUDGET-2)
    - Files: `web/src/pages/BudgetPage.tsx`

#### Tests

- [ ] **Spending trends budget data tests**
    - Effort: S
    - What: Two scenarios added to `test_get_spending_trends.py`: response includes budget amounts per group per month, months with no budget return `None` (not `0`).
    - Files: `tests/unit/application/test_get_spending_trends.py`

---

### v1.3.3: Accounting Guardrails
**Goal**: Eliminate rounding-induced drift, add runtime invariant assertions, validate settlement links, and surface pre-finalization warnings.
**Status**: Completed (2026-04-05)
**Context**: Three accounting integrity gaps found via GAAP/double-entry principles review. (1) `compute_shares` rounds both shares independently — `payer_share + other_share` can differ from `abs(amount)` by $0.01 per odd-cent transaction, accumulating silently over ~200 txs/month. (2) The reconciliation zero-sum invariant (`net_person_A + net_person_B == 0`) is guaranteed by construction but never asserted — data corruption or logic regressions would produce silently wrong balances. (3) Settlement transaction linking has no validation — double-linking (same bank tx claimed by two settlements) and same-person linking (both linked txs from one partner) are accepted without error. (4) Finalization is a blind toggle with no pre-flight checks (missing uploads, unsettled balance, unmapped categories).
**What this unlocks**: Mathematically exact reconciliation (not approximate). Runtime detection of logic bugs or data corruption. Confidence that linked settlement transactions are valid. Informed finalization decisions.

#### Backend

- [x] **Fix rounding drift in `compute_shares`**
    - Effort: S
    - What: Changed `other_share` from independent rounding to remainder computation (`abs_amount - payer_share`). Guarantees `payer_share + other_share == abs_amount` exactly.
    - Files: `src/domain/splits.py`

- [x] **Add `InvariantViolationError` exception type**
    - Effort: S
    - What: New `InvariantViolationError(DomainError)` in exceptions. Falls through to catch-all 500 handler (returns opaque response, logs full details) — not mapped in `_DOMAIN_ERROR_MAP` to avoid leaking internal state.
    - Files: `src/domain/exceptions.py`

- [x] **Add zero-sum assertion in `_compute_settlement`**
    - Effort: S
    - What: After computing `net1` and `net2`, check `net1 + net2 == Decimal(0)`. If violated, raise `InvariantViolationError`. Uses `raise`, not `assert`.
    - Files: `src/domain/reconciliation.py`

- [x] **Validate settlement transaction links**
    - Effort: S
    - What: Double-link check via `get_by_transaction_id()` + same-person check on `payer_person_id`. DB-level UNIQUE constraint on `transaction_id` as definitive guard (migration 0007).
    - Files: `src/application/use_cases/record_settlement.py`, `alembic/versions/0007_settlement_link_unique.py` (new), `src/infrastructure/persistence/models/settlement_transaction_link_model.py`

- [x] **Pre-finalization warnings in settle-up data**
    - Effort: M
    - What: Added `finalization_warnings: list[str]` to result and schema. Three checks (missing upload, unsettled balance, unmapped categories). Only when `not is_finalized`. Extracted to `_build_finalization_warnings()` helper.
    - Files: `src/application/use_cases/get_settle_up_data.py`, `src/interface/api/schemas/settlements.py`

#### Frontend

- [x] **Show finalization warnings on FinalizationBanner**
    - Effort: M
    - What: Optional `warnings` prop renders as compact `<ul>` in `text-warning-muted-foreground`. Orval types regenerated.
    - Files: `web/src/components/FinalizationBanner.tsx`, `web/src/pages/SettleUpPage.tsx`

#### Tests

- [x] **Add `compute_shares` unit tests**
    - Effort: S
    - What: 6 tests: even/odd-cent splits, asymmetric, 0%/100%, boundary. Each asserts `payer + other == abs(amount)`.
    - Files: `tests/unit/domain/test_splits.py` (new)

- [x] **Update existing rounding test assertions**
    - Effort: S
    - What: `bob_summary.total_share`: `16.67` → `16.66`. Added sum invariant check. Adjustment test updated similarly.
    - Files: `tests/unit/domain/test_reconciliation.py`, `tests/unit/domain/test_adjustments.py`

- [x] **Zero-sum assertion tests**
    - Effort: S
    - What: Mixed odd-cent amounts pass without error. White-box test with broken summaries raises `InvariantViolationError`.
    - Files: `tests/unit/domain/test_reconciliation.py`

- [x] **Settlement link validation tests**
    - Effort: S
    - What: Rejects already-linked, rejects all-same-person, allows mixed-person. 3 tests.
    - Files: `tests/unit/application/test_record_settlement.py`

- [x] **Pre-finalization warnings tests**
    - Effort: S
    - What: 5 scenarios: missing upload, unsettled balance, unmapped categories, all clear, already finalized.
    - Files: `tests/unit/application/test_get_settle_up_data.py`

---

### v1.3.4: Monetary Type Consistency + Extended Invariants
**Goal**: Close the float precision gap at API boundaries, extend zero-sum invariant checks to budget and adjustment computations, and add soft validation for settlement link amounts.
**Status**: Completed (2026-04-06)
**Context**: v1.3.3 fixed rounding drift in domain logic and added the reconciliation zero-sum assertion. But the API boundary still used `float` for monetary fields in Pydantic schemas — Decimal precision in the domain was silently converted to IEEE 754 on every request/response. Budget and adjustment computations lacked the invariant checks that reconciliation had. Settlement linking accepted any transaction regardless of amount proximity.
**What this unlocks**: End-to-end Decimal precision from JSON input through domain to JSON output. Budget computation bugs caught at runtime. Settlement mislinks flagged before they cause confusion.

#### Backend

- [x] **`MoneyField` annotated type for API schemas**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `Annotated[Decimal, PlainSerializer(float), WithJsonSchema({"type": "number"})]` — accepts Decimal on input, serializes as JSON number on output, generates `type: number` in OpenAPI spec. Replaces all `float` monetary fields across 7 schema files. Removed all `float()` casts in `from_domain`/`from_result` methods and `Decimal(str(...))` wrappers in route handlers.
    - Files: `src/interface/api/schemas/types.py` (new), `src/interface/api/schemas/reconciliation.py`, `src/interface/api/schemas/settlements.py`, `src/interface/api/schemas/transactions.py`, `src/interface/api/schemas/budgets.py`, `src/interface/api/schemas/dashboard.py`, `src/interface/api/schemas/insights.py`, `src/interface/api/schemas/uploads.py`, `src/interface/api/schemas/persons.py`, `src/interface/api/routes/transactions.py`, `src/interface/api/routes/settlements.py`

- [x] **Budget spending integrity assertion**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `_check_totals_match()` and `_assert_spending_integrity()` in budget.py verify that group status spending sums match the breakdowns they were derived from (monthly and YTD). Called in both `compute_budget_overview()` and `compute_personal_budget_overview()`. Raises `InvariantViolationError` on mismatch.
    - Files: `src/domain/budget.py`

- [x] **Adjustment zero-sum assertion**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `assert_adjustments_zero_sum()` verifies both persons' adjustments sum to zero (closed system). Currently test-only — validates the property without adding to the production export path (which computes one person at a time).
    - Files: `src/domain/export/adjustments.py`, `vulture_whitelist.py`

- [x] **Settlement link amount validation**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: Soft warning when no linked transaction amount is within 20% of the settlement amount. New `RecordSettlementResponse` wrapper returns `{settlement, warnings}`. Not a hard block — allows the couple to link intentionally mismatched transactions.
    - Files: `src/application/use_cases/record_settlement.py`, `src/interface/api/schemas/settlements.py`, `src/interface/api/routes/settlements.py`

#### Frontend

- [x] **Orval codegen regeneration**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: Regenerated TypeScript API client. All monetary fields remain `number` type (MoneyField produces `type: number` in OpenAPI). New `RecordSettlementResponse` type generated. No frontend code changes needed.
    - Files: `web/src/api/generated/**`

#### Tests

- [x] **Adjustment zero-sum property tests**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: 3 tests: single expense, mixed splits/payers/refunds, odd-penny rounding. Each computes adjustments for both persons and asserts they sum to zero.
    - Files: `tests/unit/domain/test_adjustments.py`

---

### v1.3.5: Graceful Degradation, Zero-Tolerance Type Safety, Error Handling Split
**Goal**: Demote budget integrity from hard crash to graceful degradation, eliminate all basedpyright warnings, and split the monolithic middleware into focused modules.
**Status**: Completed (2026-04-06)
**Context**: v1.3.4 added a budget spending integrity assertion that raised `InvariantViolationError` — blocking the entire Budget page with no user recovery path. basedpyright reported 45 warnings across 9 files (structlog `Any` leaks, SQLAlchemy Row `Any` attributes, argparse `Namespace` `Any`). `middleware.py` mixed request logging with error handling and used inner-function closures that required `# pyright: ignore` suppressions.
**What this unlocks**: Budget page never crashes on integrity drift — shows data with a subtle indicator. Zero basedpyright warnings. Clean separation of request logging from error handling. No suppression comments anywhere.

#### Backend

- [x] **Budget graceful degradation**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `_assert_spending_integrity` (raises) → `_check_spending_integrity` (returns `Decimal | None` drift). `BudgetOverview.spending_drift` threads through domain → use case → API. Use case logs structured warning on drift. `InvariantViolationError` import removed from budget.py.
    - Files: `src/domain/budget.py`, `src/application/use_cases/get_budget_overview.py`, `src/interface/api/schemas/budgets.py`

- [x] **structlog typed logging**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `structlog.get_logger()` → `structlog.stdlib.get_logger()` in all 7 files. Returns `BoundLogger` instead of `Any`, eliminating 35 basedpyright warnings.
    - Files: `src/application/use_cases/get_budget_overview.py`, `src/application/use_cases/preview_csv.py`, `src/application/use_cases/seed_category_groups.py`, `src/application/use_cases/seed_settlement_merchants.py`, `src/application/use_cases/upload_csv.py`, `src/interface/api/app.py`, `src/interface/api/request_logging.py`

- [x] **SQLAlchemy type fixes**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `upload_repository.py` — `result.tuples().all()` with typed `cast` instead of `Row` attribute access (16 warnings → 0). `category_group_budget_model.py` — explicit `__table_args__` type annotation (1 warning → 0).
    - Files: `src/infrastructure/persistence/repositories/upload_repository.py`, `src/infrastructure/persistence/models/category_group_budget_model.py`

- [x] **argparse type fix**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: `cast(str | None, args.command)` instead of untyped attribute access on `Namespace` (4 warnings → 0).
    - Files: `src/cli.py`

- [x] **Middleware split: request_logging + error_handling**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: Deleted `middleware.py`. Created `request_logging.py` (pure ASGI request timing) and `error_handling.py` (domain exception → HTTP mapping). Single `_handle_domain_error` with dict lookup replaces N dynamically-created closures. DRY `_error_response` envelope builder. Module-level handlers eliminate all `# pyright: ignore[reportUnusedFunction]` suppressions. `exc_info=exc` eliminates `# noqa: LOG014`.
    - Files: `src/interface/api/error_handling.py` (new), `src/interface/api/request_logging.py` (new), `src/interface/api/middleware.py` (deleted), `src/interface/api/app.py`

- [x] **Zero-tolerance rules and ship skill**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: Updated `.claude/rules/code-quality.md` (zero tolerance, no exceptions). Updated `.claude/skills/ship/SKILL.md` Step 7 (zero warnings/errors required, frontend gates included).
    - Files: `.claude/rules/code-quality.md`, `.claude/skills/ship/SKILL.md`

#### Frontend

- [x] **Budget drift indicator**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: If `spending_drift` is non-null, shows muted info note at bottom of Budget page. Non-blocking, non-alarming.
    - Files: `web/src/pages/BudgetPage.tsx`

- [x] **BudgetPage quality fix**
    - Effort: S
    - Status: Completed (2026-04-06)
    - What: Removed redundant `getHealthStyle(health)` call in `GroupHeader` — now uses the `healthStyle.label` prop already passed in.
    - Files: `web/src/pages/BudgetPage.tsx`
