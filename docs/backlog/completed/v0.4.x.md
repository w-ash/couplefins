# v0.4.x: Budget Tracking & Month Finalization

For strategic overview, see the [planning overview](../README.md).

---

### v0.4.0: Budget Tracking (Vertical Slice 1)
**Goal**: Set monthly budget limits per category group for shared expenses and track spending against them — both monthly and year-to-date.
**Context**: Beyond reconciliation, the couple wants to manage their shared spending targets. They review budgets together once a month and need both a current-month snapshot and a YTD cumulative view.
**What this unlocks**: Proactive budget awareness, not just retroactive reconciliation.
**Key tech choices**: CategoryGroupBudget entity with effective_from for versioning over time. Budgets are at the category group level, not individual category. YTD budget = monthly_amount × months elapsed.

#### Budget Management Epic

- [x] **Budget CRUD use cases + API endpoints**
    - Effort: M
    - Status: Completed (2026-03-12)
    - What: GET/POST/PUT/DELETE /api/v1/budgets — manage monthly budget limits per category group
    - Why: Backend for budget configuration
    - Dependencies: v0.2.x
    - Notes:
        - CategoryGroupBudget: group_id (FK to CategoryGroup), monthly_amount, effective_from (date)
        - Budget overview endpoint with per-group spending health (on_track/warning/over)
        - YTD prorated calculations with effective_from support
        - Full CRUD: SaveBudgetUseCase, UpdateBudgetUseCase, DeleteBudgetUseCase, ListBudgetsUseCase, GetBudgetOverviewUseCase
        - Refactored reconciliation into `domain/categories.py` and `domain/budget.py`

- [x] **Budget tracking page**
    - Effort: M
    - Status: Completed (2026-03-12)
    - What: React page to set/edit budgets per category group and see spending vs. budget
    - Why: Visual feedback on shared spending health
    - Dependencies: "Budget CRUD use cases + API endpoints"
    - Notes:
        - Monthly and YTD views with toggle
        - Progress bars with health indicators (on_track/warning/over)
        - Inline budget editing
        - Expandable category rows within groups
        - Grand total row

- [x] **Unit tests for budget logic**
    - Effort: S
    - Status: Completed (2026-03-12)
    - What: Tests for budget retrieval with spending aggregation, effective_from logic, YTD calculations
    - Why: Budget calculations must be correct
    - Dependencies: "Budget CRUD use cases + API endpoints"
    - Notes:
        - 21 domain tests + use case tests for save/update/delete/overview
        - Tests for spending rollup, effective_from, YTD aggregation, mid-year budget changes

---

### v0.4.1: Month Finalization (Vertical Slice 2)
**Goal**: Lock completed months to prevent accidental changes after both people agree.
**Context**: Once reconciliation is agreed upon, the month should be locked. The ReconciliationPeriod entity already had is_finalized, finalized_at, and notes fields from v0.1.0.
**What this unlocks**: Confidence that past months won't shift unexpectedly.

#### Finalization Epic

- [x] **Finalize/unfinalize use cases + API endpoints**
    - Effort: S
    - Status: Completed (2026-03-12)
    - What: POST /reconciliation/finalize, POST /reconciliation/unfinalize, GET /reconciliation/period-status
    - Why: Prevents accidental modification of agreed-upon reconciliation
    - Dependencies: v0.2.0
    - Notes:
        - PeriodFinalizedError domain exception mapped to 409 PERIOD_FINALIZED
        - Shared finalization guard in `_shared/finalization.py` (single + batch variants)
        - Upload CSV use case guarded — extracts affected (year, month) pairs from parsed transactions
        - Reconciliation and dashboard responses enriched with is_finalized + finalized_at
        - Dashboard month history entries include is_finalized for lock icons
        - Repository: added get_by_year for batch period status lookups (avoids N+1)

- [x] **Finalization UI on Transactions page**
    - Effort: S
    - Status: Completed (2026-03-12)
    - What: FinalizationBanner component between header and content, "Finalize" / "Unlock" actions
    - Why: User-facing control for month locking — lives where the reconciliation data is reviewed
    - Dependencies: "Finalize/unfinalize use cases + API endpoints"
    - Notes:
        - Reusable FinalizationBanner component with lock/unlock states
        - Finalized state: teal primary-muted background, lock icon, formatted date, understated "Unlock" ghost button
        - Open state: subtle bordered row with teal "Finalize" pill button
        - useMutation for finalize/unfinalize, invalidates reconciliation + dashboard queries

- [x] **Finalization indicators on Dashboard**
    - Effort: S
    - Status: Completed (2026-03-12)
    - What: FinalizationBanner on dashboard + lock icons in month history table
    - Why: At-a-glance history of which months are locked
    - Dependencies: "Finalization UI on Transactions page", v0.2.2 "Dashboard page"
    - Notes:
        - Same FinalizationBanner reused on dashboard page
        - Lock icon (lucide Lock, size-3) next to month name in history table for finalized months
        - Upload page: no proactive check — 409 API error displays naturally via existing error handling
