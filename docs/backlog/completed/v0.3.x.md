# v0.3.x: Export & Adjustments

For strategic overview, see the [planning overview](README.md).

---

### v0.3.0: Adjustment Export Engine (Vertical Slice 1)
**Goal**: Generate per-person Monarch-compatible adjustment CSVs from shared transactions for any reconciled month.
**Context**: With reconciliation working (v0.2.x), users can see who owes whom. But their individual Monarch accounts still show only what they personally paid. This version generates adjustment entries so each person's Monarch reflects their true share of shared expenses, by category.
**What this unlocks**: Complete lifecycle — the couple's Monarch accounts accurately reflect their true personal spending after importing adjustments.
**Key tech choices**: Pure domain functions for adjustment math and CSV rendering. Deterministic dedup IDs via UUID5 for Monarch re-import safety. No stored adjustment entities — adjustments are always computed fresh from current transaction data.

#### Use Cases

**UC1: "I want to export my adjustments for March"**
The user selects a month, clicks export, and gets a CSV. For every shared transaction where they were the payer, the CSV includes a credit for their partner's share (reducing their apparent category spend). For every shared transaction where their partner was the payer, the CSV includes a debit for their own share (adding to their apparent category spend). The result: after importing into Monarch, their spending-by-category is accurate.

**UC2: "I accidentally imported the same CSV twice into Monarch"**
Each adjustment row's Notes field contains a deterministic dedup ID (e.g., `[cf:a1b2c3d4e5f6]`). The user can search Monarch Notes for `cf:` to find and delete duplicates. The dedup ID is derived from the source transaction ID + target person ID, so it's stable across re-exports of the same data.

**UC3: "My partner re-uploaded their CSV — I need fresh adjustments"**
Re-uploading source transactions replaces them (new transaction IDs). Re-exporting now produces different dedup IDs (correct — source data changed). The user should delete old `cf:` adjustments in Monarch and re-import the new CSV.

**UC4: "I want to re-export and verify nothing changed"**
If no source data has changed, re-exporting produces a byte-identical CSV (same rows, same sort order, same dedup IDs). This is the core idempotency guarantee.

#### Idempotency Design

| Operation | Mechanism | Guarantee |
|-----------|-----------|-----------|
| Import (Monarch → Couplefins) | `delete_by_upload_id` + re-insert | Re-upload replaces all transactions cleanly |
| Export computation | Pure function, deterministic sort | Same input → identical output |
| Export dedup IDs | `uuid5(namespace, tx_id:person_id)` | Same source data → same IDs; changed source → changed IDs |
| Monarch re-import | `[cf:xxxx]` in Notes field | User can search Notes for `cf:` to find/delete duplicates |

#### Person Configuration Epic

- [x] **Add `adjustment_account` field to Person**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: Add `adjustment_account: str` to Person entity. This is the Monarch account name where adjustments are imported (e.g., "Shared Expense Adjustments"). The couple creates this as a manual account in Monarch.
    - Why: Required for the Account column in generated CSVs. Monarch needs an account to associate imported transactions with.
    - Dependencies: v0.1.0 (data model)
    - Notes:
        - Entity: add field to `src/domain/entities/person.py`
        - Model: add column to PersonModel, default empty string
        - Repository: update `_to_domain` / `_to_model` converters
        - Migration: add column with server_default="" for existing rows
        - Factory: update `make_person` with `adjustment_account` param
        - API schema: update person endpoints to accept/return the field
        - Added PATCH `/persons/{id}` endpoint with `UpdatePersonUseCase` for updating the field
        - Uses `attrs.evolve` for immutable entity updates

- [x] **Person settings UI for adjustment account**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: UI in the Settings page (`/settings`) for each person to configure their Monarch adjustment account name
    - Why: Must be set before export can work
    - Dependencies: "Add `adjustment_account` field to Person", v0.1.3 (Settings stub page)
    - Notes:
        - `PersonAccountSettings` component with per-person inline form
        - Save button with dirty detection, success feedback (checkmark), error display
        - Proper cleanup of setTimeout via useEffect

#### Adjustment Engine Epic

- [x] **Implement adjustment calculation domain logic**
    - Effort: M
    - Status: Completed (2026-03-11)
    - What: Pure function `compute_adjustments(shared_transactions, target_person) → list[Adjustment]` that computes per-person adjustment entries from shared transactions
    - Why: Core business logic — must be pure, deterministic, and thoroughly tested
    - Dependencies: v0.1.0 (domain entities)
    - Notes:
        - Value object `Adjustment` (frozen attrs): `dedup_id`, `source_transaction_id`, `date`, `merchant`, `category`, `amount` (Decimal), `account`
        - File: `src/domain/export/adjustments.py`
        - Split math extracted to shared `compute_shares()` in `src/domain/splits.py` (also used by `reconciliation.py`)
        - Dedup ID: `uuid5(COUPLEFINS_NAMESPACE, f"{tx.id}:{target_person.id}").hex[:12]`
        - Deterministic sort: by `(date, merchant, category, source_transaction_id)`

- [x] **Unit tests for adjustment calculator**
    - Effort: M
    - Status: Completed (2026-03-11)
    - What: Comprehensive tests for adjustment math, idempotency, and edge cases
    - Why: Financial math must be exact. Idempotency must be provable by test.
    - Dependencies: "Implement adjustment calculation domain logic"
    - Notes:
        - 13 tests covering: 50/50 split, 70/30 split, 100/0 (skips zero share), 0/100 (full amount), refunds, dedup ID determinism, dedup IDs differ per person, empty transactions, rounding fractional cents, deterministic sort order, mixed expenses and refunds, non-shared transactions skipped, account from target person

- [x] **Implement Monarch CSV renderer**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: Pure function `render_adjustment_csv(adjustments) → str` that produces a Monarch-importable CSV string
    - Why: Formats adjustment rows into the exact CSV format Monarch expects for import
    - Dependencies: "Implement adjustment calculation domain logic"
    - Notes:
        - File: `src/domain/export/csv_renderer.py`
        - Columns: Date, Amount, Merchant, Category, Account, Tags, Notes
        - Tags: `couplefins-adjustment` constant
        - Notes: `[cf:xxxxxxxxxxxx]` dedup ID
        - Account sourced from `Adjustment.account` (set by `compute_adjustments` from target person)

- [x] **Unit tests for CSV renderer**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: Tests for CSV format correctness and idempotency
    - Why: CSV must be valid for Monarch import
    - Dependencies: "Implement Monarch CSV renderer"
    - Notes:
        - 7 tests: header and row structure, empty adjustments (header only), positive amount formatting, special characters escaped (csv.reader round-trip), idempotent output, order preservation, notes format

#### Export API Epic

- [x] **Export adjustments use case**
    - Effort: M
    - Status: Completed (2026-03-11)
    - What: Application use case that orchestrates fetching shared transactions, computing adjustments, and rendering CSV for a given person and period
    - Why: Wires domain logic to infrastructure, following existing `execute_use_case` pattern
    - Dependencies: "Implement Monarch CSV renderer", v0.2.0 (reconciliation engine — shared transactions exist)
    - Notes:
        - File: `src/application/use_cases/export_adjustments.py`
        - Read-only use case — no `uow.commit()` call
        - Validates: exactly 2 persons, target person exists, adjustment_account configured
        - Returns: `ExportAdjustmentsResult` with csv_content, filename, person_name, adjustment_count

- [x] **Export API endpoint**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: `GET /api/v1/persons/{person_id}/export/{year}/{month}` — returns Monarch-importable CSV as file download
    - Why: Serves the export to the frontend as a downloadable file
    - Dependencies: "Export adjustments use case"
    - Notes:
        - Response: `text/csv` with `Content-Disposition: attachment; filename="adjustments-{name}-{year}-{month:02d}.csv"`
        - Row count 0: return headers-only CSV (valid empty file)
        - Errors map through existing exception middleware (404, 422)

- [x] **Unit tests for export use case**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: Use case tests with mock UoW
    - Why: Validate orchestration logic and error handling
    - Dependencies: "Export adjustments use case"
    - Notes:
        - 6 tests: happy path, person not found, no couple (validation error), empty adjustment_account, empty month (header-only CSV), read-only (commit never called)

- [x] **Integration tests for export endpoint**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: Full API tests for the export endpoint using httpx AsyncClient
    - Why: Validates the full pipeline from HTTP request to CSV response
    - Dependencies: "Export API endpoint"
    - Notes:
        - 6 tests: full flow (both persons upload + export), Bob's perspective (account name in rows), person not found (404), without adjustment account (422), empty month (header only), idempotent re-export

---

### v0.3.1: Export UI (Vertical Slice 2)
**Goal**: Users can download their adjustment CSV directly from the transactions page.
**Context**: The API exists (v0.3.0), now add the UI trigger.
**What this unlocks**: Self-service export without needing to call the API directly.

#### Export UI Epic

- [x] **Export button on transactions page**
    - Effort: M
    - Status: Completed (2026-03-11)
    - What: "Download My Adjustments" button per person on the transactions page. Triggers CSV download for the selected month.
    - Why: Primary user interaction for the export feature
    - Dependencies: v0.3.0 ("Export API endpoint"), v0.2.0 (transactions page)
    - Notes:
        - Two buttons (one per person) or a person selector + single button
        - Disabled state: muted styling with inline explanation text ("Set adjustment account in Settings to export") — not tooltip-only (touch devices have no hover)
        - Shows row count after download ("Exported 12 adjustment rows")
        - Preview-before-commit pattern: show adjustment preview with real row counts before triggering download
        - Added `PreviewAdjustmentsUseCase` + `GET /persons/{id}/adjustments/{year}/{month}` JSON endpoint (DRY refactor: extracted `_load_adjustments()` shared helper with `ExportAdjustmentsUseCase`)
        - Download uses raw `fetch()` + blob since CSV endpoint returns `text/csv` (not JSON)
        - Fixed pre-existing SettingsPage test drift from v0.3.0 (PersonAccountSettings replaced placeholder text)

- [x] **Adjustment preview component**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: Expandable section showing adjustment rows before download — merchant, category, amount, type (credit/debit)
    - Why: Builds confidence before importing into Monarch
    - Dependencies: "Export button on transactions page"
    - Notes:
        - Progressive disclosure: CSS grid `0fr→1fr` transition for expand/collapse
        - Weight tiers: merchant=primary, category=tertiary, amount=secondary (right-aligned, `tabular-nums`)
        - Credit/debit type: ArrowUpRight/ArrowDownLeft icon + color + "Credit"/"Debit" text label (never color alone)
        - Lazy-loaded via `useQuery` with `enabled: expanded` — data only fetched when preview is opened
        - Also fixed UploadPage field diff to use ArrowRight icon between old→new values (was color-only)
