# v0.2.x: Reconciliation

For strategic overview, see the [planning overview](README.md).

---

### v0.2.0: Reconciliation Engine + Transactions Page (Vertical Slice 1)
**Goal**: Answer the question "who owes whom this month?" — the core reason this app exists.
**Context**: Both people upload their Monarch CSVs (v0.1.x). Now they need to see the result: a single settlement number, a full transaction list, and category breakdowns that help them understand where their shared money went.
**What this unlocks**: Replaces the spreadsheet for monthly reconciliation.
**Key tech choices**: Pure domain logic for reconciliation math (Decimal, round-per-transaction). Single fat API endpoint (all data for the page in one call). URL-driven month selection (`?year=&month=`).

#### User Journey

The monthly reconciliation cycle:
1. Month ends. Each person exports their Monarch CSV and uploads it.
2. Both uploads land. They navigate to Transactions for that month.
3. **First question**: "Who owes whom, and how much?" — the hero settlement number.
4. **Second question**: "Is this complete? Did both of us upload?" — upload status banner.
5. **Third question**: "Where did the money go?" — category group breakdown.
6. **Fourth question**: "Do any of these transactions look wrong?" — full transaction table.
7. One person Venmos the other. Done.

#### Information Hierarchy

| Priority | Element | Why |
|---|---|---|
| **Hero** | Settlement card: "Alice owes Bob $127.50" | The single answer they came for |
| **Gating** | Upload status banner (if incomplete) | Prevents "why does the number look wrong?" |
| **Context** | Month selector | Navigate to past months |
| **Summary** | Stats row: total shared, Alice paid X, Bob paid Y | Orientation before detail |
| **Analysis** | Category group breakdown (collapsible → categories) | "Where did the money go?" |
| **Detail** | Transaction table, sorted by date desc | Review individual expenses |
| **Warning** | Unmapped categories (only if present) | Actionable: link to Settings |

#### Edge Cases

| Scenario | What the user sees |
|---|---|
| **Only one person uploaded** | Banner: "Partial data — only Alice has uploaded for March 2026." Summary computes with available data but is clearly labeled incomplete. |
| **Zero shared transactions** | Empty state: "No shared transactions for March 2026." Not an error — maybe everything was personal this month. |
| **No data at all** | Same as zero shared transactions. |
| **Unmapped categories** | Warning banner listing them. Transactions still appear with group column showing "Uncategorized." Don't block reconciliation. |
| **Refunds** (positive amounts) | Math works naturally with signed convention. Shared $20 refund at 50/50 means each person's share of the refund is $10. UI labels positive amounts in teal. |
| **100/0 split** (`s100`) | Payer covers 100%, other owes nothing. Displays "100 / 0" in split column. |
| **0/100 split** (`s0`) | Payer covers 0%, other owes everything. Displays "0 / 100." |
| **Net zero** | Settlement card: "All settled!" |

---

#### Reconciliation Engine Epic

- [x] **Implement reconciliation domain logic**
    - Effort: M
    - Status: Completed (2026-03-10)
    - What: Pure function `reconcile()` in `src/domain/reconciliation.py` — takes shared transactions, persons, category mappings, and category groups → returns `ReconciliationSummary`
    - Why: Core business logic — must be pure and thoroughly tested
    - Dependencies: v0.1.x
    - Notes:
        - **New file**: `src/domain/reconciliation.py` — value objects + pure reconcile function
        - Value objects (frozen attrs, not persisted entities): `PersonSummary`, `SettlementResult`, `CategoryBreakdown`, `CategoryGroupBreakdown`, `ReconciliationSummary`
        - **Math per transaction**: `abs_amount = abs(tx.amount)`, `payer_share = abs_amount × payer_percentage / 100`, `other_share = abs_amount × (100 - payer_percentage) / 100`
        - **Settlement**: For each person, compute `net_owed = total_share - total_paid`. The person with positive net_owed pays the other. For refunds (positive amount), the payer "overpaid" by a negative amount, reducing what the non-payer owes.
        - **Rounding**: Use `Decimal` throughout. Quantize each per-transaction share to `Decimal("0.01")` with `ROUND_HALF_UP`. Round per-transaction, not in aggregate.
        - **Category grouping**: Build `dict[str, tuple[UUID | None, str]]` lookup from mappings + groups. Unmapped categories get `(None, "Uncategorized")`. Group transactions by category, then roll up into category groups sorted by total amount desc.
        - **Function signature**: `reconcile(transactions: list[Transaction], persons: list[Person], category_mappings: list[CategoryMapping], category_groups: list[CategoryGroup]) -> ReconciliationSummary`
        - **Helpers** (private): `_build_category_lookup`, `_compute_person_summaries`, `_compute_settlement`, `_compute_category_breakdowns`

- [x] **Unit tests for reconciliation engine**
    - Effort: M
    - Status: Completed (2026-03-10)
    - What: Comprehensive tests for reconciliation math
    - Why: Financial math must be exact — pennies matter
    - Dependencies: "Implement reconciliation domain logic"
    - Notes:
        - **New file**: `tests/unit/domain/test_reconciliation.py`
        - Use existing factories from `tests/fixtures/`
        - Test cases:
            - `test_equal_split_both_paid_equally` — net zero settlement
            - `test_equal_split_one_person_paid_everything` — one person owes the other
            - `test_unequal_split_70_30`
            - `test_mixed_splits_multiple_transactions` — realistic month
            - `test_refund_reduces_settlement` — positive amount with shared tag
            - `test_100_0_split_contributes_nothing_to_other`
            - `test_0_100_split_full_amount_to_other`
            - `test_empty_transactions_returns_zeroed_summary`
            - `test_single_transaction`
            - `test_category_group_aggregation` — multiple categories in same group
            - `test_unmapped_category_appears_as_uncategorized`
            - `test_rounding_cents` — fractional cents from percentage math
            - `test_all_refunds_no_expenses`

- [x] **Reconciliation use case + API endpoint**
    - Effort: M
    - Status: Completed (2026-03-10)
    - What: `GET /api/v1/reconciliation?year=YYYY&month=MM` — single endpoint returning everything the Transactions page needs
    - Why: Serves the reconciliation data to the frontend. One call = one render. No waterfall.
    - Dependencies: "Implement reconciliation domain logic"
    - Notes:
        - **New files**:
            - `src/application/use_cases/get_reconciliation.py` — `GetReconciliationCommand` (year, month with validators), `GetReconciliationResult`, `GetReconciliationUseCase`
            - `src/interface/api/routes/reconciliation.py` — thin GET handler
            - `src/interface/api/schemas/reconciliation.py` — Pydantic response models with `from_result` classmethod
        - **Modify**: `src/interface/api/app.py` — register reconciliation router
        - **Use case orchestration**: Fetch persons, shared transactions for period, category mappings, category groups, uploads for period. Call `reconcile()`. Build upload status per person. Detect unmapped categories.
        - **Upload status** in response: per-person `{ person_id, person_name, has_uploaded, upload_count }` — critical for the "partial data" banner
        - **Unmapped categories** in response: list of category names from transactions that have no group mapping
        - **Response includes the full transaction list** — the table needs it and it's <50KB for 200 transactions
        - **Tests**: `tests/unit/application/test_get_reconciliation.py` (use case with mocked UoW), `tests/integration/api/test_reconciliation.py` (full HTTP round-trip: setup couple → upload CSVs → GET reconciliation → verify shape)

#### Transactions UI Epic

- [x] **Extract shared formatters from UploadPage**
    - Effort: XS
    - Status: Completed (2026-03-10)
    - What: Move `formatDate`, `formatCurrency`, `MONTHS`, `currentYear`, `currentMonth` from UploadPage into `web/src/lib/format.ts`. Update UploadPage imports.
    - Why: TransactionsPage needs the same formatters. DRY.
    - Dependencies: v0.1.x

- [x] **MonthSelector component**
    - Effort: S
    - Status: Completed (2026-03-10)
    - What: Reusable month/year picker that syncs with URL search params (`useSearchParams`). Two `<select>` elements (month dropdown, year input). Defaults to current month/year.
    - Why: Needed by Transactions page now, Dashboard page in v0.2.1. Build once.
    - Dependencies: "Extract shared formatters from UploadPage"
    - Notes:
        - **New file**: `web/src/components/MonthSelector.tsx`
        - URL-driven: `/transactions?year=2026&month=3` — bookmarkable, shareable
        - Uses shared `MONTHS` array and `currentYear`/`currentMonth` from `lib/format.ts`
        - **Test**: `web/src/components/MonthSelector.test.tsx`

- [x] **Transactions page**
    - Effort: L
    - Status: Completed (2026-03-10)
    - What: React page at `/transactions` showing month's reconciliation with settlement card, summary stats, category breakdown, and transaction table
    - Why: Primary user-facing view — replaces the spreadsheet
    - Dependencies: "Reconciliation use case + API endpoint", "MonthSelector component", "Extract shared formatters"
    - Notes:
        - **New files**:
            - `web/src/pages/TransactionsPage.tsx` — main page component
            - `web/src/lib/reconciliation.ts` — TypeScript types + `fetchReconciliation` function
        - **Modify**:
            - `web/src/App.tsx` — add `/transactions` route
            - `web/src/components/Sidebar.tsx` — enable Transactions nav item
        - **Page structure** (top to bottom):
            1. Page header with MonthSelector
            2. UploadStatusBanner (conditional — only if not both uploaded)
            3. SettlementCard — hero element, largest text: "Alice owes Bob $127.50" or "All settled!" Person names use accent colors.
            4. SummaryStats row — 3 compact cards: total shared spending, Alice paid $X, Bob paid $Y
            5. CategoryGroupBreakdown — collapsible rows per group, sorted by total desc. Click to expand individual categories.
            6. TransactionTable — date desc default sort. Columns: date, merchant, category, group, paid by (accent-colored), amount (coral for expense, teal for refund), split ratio, each person's share
        - **Data fetching**: `useQuery` with key `["reconciliation", year, month]`. Single `apiFetch` call.
        - **Design system**: semantic tokens throughout, `tabular-nums` on all financial figures, right-aligned numbers
        - **Empty states**: "No shared transactions for March 2026" with guidance
        - **No pagination** — 100-200 rows max per month
        - **No client-side sorting** — default date desc is sufficient for v0.2.0
        - **Tests**: `web/src/pages/TransactionsPage.test.tsx` — render with mock data, empty state, partial upload banner, settlement display

#### Idempotent Uploads Epic

- [x] **Dedup-aware upload flow**
    - Effort: M
    - Status: Completed (2026-03-10)
    - What: Re-uploading a CSV is idempotent. Transactions are matched by natural key (date, amount, account, original_statement, payer_person_id). New transactions are inserted, unchanged are skipped, and changed transactions (e.g., updated tags or categories in Monarch) are surfaced for review. Month/year inputs removed — dates come from the CSV.
    - Why: Users re-export from Monarch after editing tags/categories. Without dedup, every re-upload creates duplicates.
    - Dependencies: "Reconciliation use case + API endpoint"
    - Notes:
        - Pure domain classification in `src/domain/dedup.py`
        - Upload entity no longer has period fields — uploads can span months
        - Preview shows new/unchanged/changed counts with field-level diffs
        - Review step in UI with per-row accept/reject checkboxes (all accepted by default)
        - Upload accepts `accepted_change_ids` to control which changes apply
        - Unique constraint on natural key prevents DB-level duplicates

---

### v0.2.1: Auto-Create Categories from CSV + Category Management UI (Vertical Slice 2)
**Goal**: Categories become first-class entities — auto-created during CSV upload, manageable via a Settings UI.
**Context**: Previously, categories were only recognized if pre-seeded in the JSON fixture. New categories from Monarch CSVs showed as "unmapped" with no way to fix them in-app.
**What this unlocks**: Users can manage category groups and mappings entirely through the UI. New categories from CSV uploads are automatically tracked.
**Key tech choices**: `CategoryMapping.group_id` nullable (`None` = known but unmapped). Upload auto-creates mappings; preview reports without creating. Delete group nullifies mappings instead of deleting them.

#### Backend Changes

- [x] **Nullable group_id on CategoryMapping**
    - Effort: S
    - Status: Completed (2026-03-10)
    - What: `group_id: UUID | None` on entity, model, and repository. Migration `d9e4f5a6b7c8`. Added `get_unmapped()` and renamed `delete_by_group_id` → `unmap_by_group_id` (UPDATE SET NULL instead of DELETE).

- [x] **Split shared helpers + auto-create on upload**
    - Effort: S
    - Status: Completed (2026-03-10)
    - What: `find_unmapped_categories` split into `find_new_categories` (no DB row) + `find_unmapped_categories` (DB row with `group_id=None`). Upload creates `CategoryMapping(group_id=None)` for new categories. Preview reports both types without persisting. Reconciliation filters `group_id=None` to "Uncategorized" fallback.

- [x] **List unmapped simplified + delete group nullifies**
    - Effort: S
    - Status: Completed (2026-03-10)
    - What: `ListUnmappedCategoriesUseCase` uses `get_unmapped()` directly. `DeleteCategoryGroupUseCase` calls `unmap_by_group_id` (categories survive group deletion).

#### Frontend Changes

- [x] **Category management settings UI**
    - Effort: M
    - Status: Completed (2026-03-10)
    - What: `CategoryMappingEditor` component with four states (loading/empty/error/data). Unmapped categories banner with per-category group assignment dropdowns. Expandable group cards with inline rename and delete (confirmation dialog). Add group form. All mutations invalidate category query keys.

- [x] **Upload cache invalidation**
    - Effort: XS
    - Status: Completed (2026-03-10)
    - What: Upload success invalidates `category-groups` and `unmapped-categories` query keys.

---

### v0.2.2: Dashboard + Month Navigation (Vertical Slice 3)
**Goal**: A landing page showing current month summary with easy navigation to past months.
**Context**: Users need a quick overview without navigating to the full transactions page. Currently the app opens to `/transactions` — a detail view with no orientation. The dashboard gives instant answers: "who owes whom this month?" and "is this month complete?"
**What this unlocks**: At-a-glance status for the current month. Natural landing page. Month history for navigating to past reconciliations.
**Key tech choices**: Single fat API endpoint (like reconciliation). Reuse `reconcile()` for both current month and YTD. New `get_distinct_periods()` repo method for month history. Budget health deferred to v0.4.0 (budgets don't exist yet).

#### User Journey

1. Open the app. Dashboard loads with current month front and center.
2. **First question**: "Who owes whom?" — hero settlement card.
3. **Second question**: "Did we both upload?" — upload status indicators.
4. **Third question**: "How does this month compare?" — YTD running total.
5. **Fourth question**: "What about past months?" — month history list, click to navigate to Transactions.
6. Quick actions: "Upload CSV" or "View Transactions" for the current month.

#### Backend Epic

- [x] **Add `get_shared_by_year` to transaction repository**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: New method on `TransactionRepositoryProtocol` and SQLAlchemy implementation. Fetches all shared transactions for a given year.
    - Why: Dashboard fetches all year's transactions in one call, then partitions by month in memory — simpler and fewer DB round-trips than the originally planned `get_distinct_periods` + per-month queries.
    - Dependencies: v0.2.0
    - Notes:
        - **Deviation from plan**: Originally planned `get_distinct_periods()` and `get_shared_by_year_through_month()`. The implementation uses a single `get_shared_by_year()` instead — the use case partitions by month and filters for YTD in Python. With ~100-200 transactions/month this is instant and avoids multiple DB round-trips.
        - **Modify**: `src/domain/repositories/transaction_repository.py` — added `get_shared_by_year(self, year: int) -> list[Transaction]`
        - **Modify**: `src/infrastructure/persistence/repositories/transaction_repository.py` — `startswith` filter on date string prefix, consistent with `get_shared_by_period`

- [x] **Dashboard use case**
    - Effort: M
    - Status: Completed (2026-03-11)
    - What: `GetDashboardUseCase` — aggregates current month reconciliation, YTD reconciliation, month history with per-month settlement amounts, and upload statuses.
    - Why: Single use case = single API call = single render. No waterfall.
    - Dependencies: "Add `get_shared_by_year` to transaction repository"
    - Notes:
        - **New file**: `src/application/use_cases/get_dashboard.py`
        - `GetDashboardCommand`: `year: int`, `month: int` (with validators). Defaults to current month on the API side.
        - `MonthHistoryEntry` (value object): `year`, `month`, `total_shared_spending: Decimal`, `settlement_amount: Decimal`, `settlement_from_person_id: UUID | None`, `settlement_to_person_id: UUID | None`
        - `GetDashboardResult`: `current_month: ReconciliationSummary`, `upload_statuses: list[UploadStatus]`, `ytd_total_shared_spending: Decimal`, `ytd_settlement: SettlementResult | None`, `month_history: list[MonthHistoryEntry]`, `persons: list[Person]`, `unmapped_categories: list[str]`
        - **Current month**: Reuse `reconcile()` — extracted from pre-computed per-month summaries to avoid redundant computation.
        - **YTD**: Filter year transactions to `month <= command.month`, then `reconcile()`.
        - **Month history**: `_reconcile_all_months()` computes each month's reconciliation once. Current month summary reused from this dict. History entries built from the same summaries.
        - **Shared helpers**: Extracted `UploadStatus` + `build_upload_statuses()` to `_shared/upload_status.py` for reuse with `GetReconciliationUseCase`.
        - **Deviation from plan**: Named `MonthHistoryEntry` (not `MonthSummary`). Uses UUID person IDs instead of direction strings — frontend resolves names. Skipped `both_uploaded` on history entries (upload status is only relevant for the current month).
        - **Test**: `tests/unit/application/test_get_dashboard.py` — happy path, empty month, multi-month history, YTD aggregation, YTD excludes future months, settlement history entries

- [x] **Dashboard API endpoint**
    - Effort: S
    - Status: Completed (2026-03-11)
    - What: `GET /api/v1/dashboard?year=YYYY&month=MM` — thin handler, delegates to `GetDashboardUseCase`. Year/month optional, defaults to current.
    - Why: Powers the landing page
    - Dependencies: "Dashboard use case"
    - Notes:
        - **New files**:
            - `src/interface/api/routes/dashboard.py` — thin GET handler (5 lines)
            - `src/interface/api/schemas/dashboard.py` — Pydantic response models with `from_result` classmethod
        - **Modify**: `src/interface/api/app.py` — register dashboard router
        - Response reuses `UploadStatusResponse` and `PersonSummaryResponse` from reconciliation schemas
        - `MonthHistoryEntryResponse`: `year`, `month`, `total_shared_spending: float`, `settlement_amount: float`, `settlement_from_person_id: UUID | None`, `settlement_to_person_id: UUID | None`
        - **Test**: `tests/integration/api/test_dashboard.py` — full HTTP round-trip: setup couple → upload CSVs → GET dashboard → verify shape, month history, YTD aggregation, defaults to current month, empty month

#### Frontend Epic

- [x] **Dashboard page**
    - Effort: L
    - Status: Completed (2026-03-11)
    - What: Landing page at `/` with current month summary card, YTD card, upload status, quick actions, and month history list. Becomes the default route.
    - Why: First thing users see — should give instant orientation
    - Dependencies: "Dashboard API endpoint"
    - Notes:
        - **New files**:
            - `web/src/pages/DashboardPage.tsx` — main page component
            - `web/src/pages/DashboardPage.test.tsx` — tests
            - `web/src/lib/dashboard.ts` — TypeScript types + `fetchDashboard` function
        - **Modify**:
            - `web/src/App.tsx` — add `/` route pointing to DashboardPage, change default from `/transactions`
            - `web/src/components/Sidebar.tsx` — enable Dashboard nav item
        - **Page structure** (top to bottom):
            1. Page header: "Dashboard" + MonthSelector
            2. SettlementCard — hero element with person accent colors
            3. UploadStatus row — per-person: CheckCircle2/Clock icon + accent-colored name pill + text
            4. SummaryStats — 3 cards: "This month", "Year to date", "YTD balance"
            5. QuickActions — "Upload CSV" → `/upload`, "View Transactions" → `/transactions?year=X&month=Y`
            6. MonthHistory — clickable table rows navigate to `/transactions?year=X&month=Y`, `tabular-nums`, right-aligned, newest first
        - **Four states**: loading (spinner), empty ("No uploads yet" with upload link), error (alert), data
        - **Data fetching**: `useQuery` with key `["dashboard", year, month]`. Single `fetchDashboard` call.
        - **Tests**: settlement card, upload status indicators, empty state, month history rows, summary stats, partial upload status, month history navigation
        - **Additional work**: `Button` component (`web/src/components/Button.tsx`) — shared button with variant/size/loading/icon props. UploadPage refactored to use `ActionPanel` component with two-column layout. `UploadStatus` extracted to `_shared/upload_status.py` for reuse.
