# v1.2.x: Scoped Dashboard, Terminology Cleanup & Settlement Linking

For strategic overview, see the [planning overview](README.md).

---

### v1.2.0: Scoped Dashboard
**Goal**: Add household/personal/all scope toggle to the Dashboard, with self-documenting metrics that always explain what's included, the time period, and why the number is useful.
**Status**: Completed (2026-04-01)
**Context**: The dashboard was household-only with no scope awareness. Budget and Transactions pages already had scope toggles. Each scope changes the dashboard's purpose: household = "how are we doing together?" (settlement-oriented), personal = "how am I doing?" (spending-health-oriented), all = "what's the full picture?" (observational). Metric labels like "This month" were ambiguous — unclear whether they included personal spending, household-no-split transactions, or just settlements. Progressive disclosure research confirmed every metric needs a contextual description layer.
**What this unlocks**: The dashboard becomes scope-aware and self-documenting. Personal scope surfaces budget alerts and per-person spending. All scope shows total financial footprint. Every stat card has a description explaining exactly what it includes.

#### Backend

- [x] **Scope parameter on dashboard endpoint**
    - Effort: M
    - Status: Completed (2026-04-01)
    - What: Added `scope` query param (household/personal/all) and `person_id` to `GetDashboardCommand`. Extracted `Scope` type alias to `_shared/command_validators.py`. Household scope returns existing data. Personal scope computes per-person spending (share of household + personal), personal month history, and budget alerts via `compute_personal_budget_overview`. All scope computes total spending across all transaction types. Promoted `compute_person_share` to public in `budget.py`.
    - Files: `src/application/use_cases/get_dashboard.py`, `src/interface/api/routes/dashboard.py`, `src/interface/api/schemas/dashboard.py`, `src/domain/budget.py`, `src/application/use_cases/_shared/command_validators.py`

- [x] **True household spending metric (not just split transactions)**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: Added `household_spending_month` and `household_spending_ytd` fields — sum of ALL `household=true` transactions regardless of `payer_percentage`. Previously the "Household spending" stat used `net_shared_spending` from reconciliation, which excluded household-no-split transactions (payer_percentage=100). The reconciliation numbers are preserved for settlement math.
    - Files: `src/application/use_cases/get_dashboard.py`, `src/interface/api/schemas/dashboard.py`

#### Frontend

- [x] **Dashboard scope toggle**
    - Effort: M
    - Status: Completed (2026-04-01)
    - What: `SegmentedControl` with Household / My Spending / All. `useDashboardFilters()` hook manages scope via URL param. Three stat components (`HouseholdStats`, `PersonalStats`, `AllStats`) render different metrics per scope. Settlement hero reframes as "You owe" / "owes you" in personal scope. Quick actions change per scope (personal swaps Settle Up for View Budget). Scope propagates to navigation links (`?scope=personal`).
    - Files: `web/src/pages/DashboardPage.tsx`, `web/src/lib/dashboard-filters.ts`

- [x] **Budget alerts card (personal scope)**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: Compact card showing top 5 categories near/over budget with progress bars and health colors. Only shown in personal scope when alerts exist. Links to `/budget?scope=personal`.
    - Files: `web/src/pages/DashboardPage.tsx`

- [x] **Personal month history**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: Alternative month history table for personal scope — columns: Month, My Spending, Breakdown (household / personal split in muted text). Row click navigates to `/transactions?scope=personal`.
    - Files: `web/src/pages/DashboardPage.tsx`

- [x] **Self-documenting metric descriptions**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: Added `description` field to `StatsGrid` component. Every stat card now has a subtitle explaining what's included and the time period (e.g., "All expenses in the household budget, Mar 2026"). Dynamic time labels via `useTimeLabels()`.
    - Files: `web/src/components/StatsGrid.tsx`, `web/src/pages/DashboardPage.tsx`

---

### v1.2.1: "Shared" → "Household" Terminology Cleanup
**Goal**: Replace "shared" as a domain concept with "household" throughout the codebase. "Shared" should only refer to the Monarch CSV tag name.
**Status**: Completed (2026-04-01)
**Context**: The codebase evolved from a "shared expense" mental model to a two-dimension model (`household` flag + `payer_percentage`). The v0.9.0 refactor introduced the `household` field but left "shared" in domain entities, API schemas, variable names, and UI labels. The `TransactionType` enum (`shared/spotted/household/personal`) is a vestige of the old model — it maps combinations of the two orthogonal fields into named types, but the two fields ARE the model. "Shared" is not a type — it's just `household=true` with a non-100% split.
**What this unlocks**: Terminology matches the actual data model. No more confusion about whether "shared spending" means "split transactions" or "all household expenses."

#### Backend

- [x] **Domain layer renames**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: `ReconciliationSummary` fields: `total_shared_spending` → `total_household_spending`, `total_shared_refunds` → `total_household_refunds`, `net_shared_spending` → `net_household_spending`. `CategoryGroupBudgetStatus.shared_spending` → `household_spending`. `_shared_expenses()` → `_household_expenses()`. Local variables `shared` → `household`, `group_shared` → `group_household`.
    - Files: `src/domain/reconciliation.py`, `src/domain/budget.py`, `src/domain/insights.py`

- [x] **Application + API schema renames**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: `MonthHistoryEntry.total_shared_spending` → `total_household_spending`. `PersonalMonthHistoryEntry.shared_portion` → `household_portion`. All API response fields containing "shared" → "household". Vulture whitelist updated.
    - Files: `src/application/use_cases/get_dashboard.py`, `src/interface/api/schemas/dashboard.py`, `src/interface/api/schemas/reconciliation.py`, `src/interface/api/schemas/budgets.py`, `vulture_whitelist.py`

#### Frontend

- [x] **Delete `TransactionType` enum and `ClassificationBadge`**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: Deleted `web/src/lib/transaction-classification.tsx`. Replaced `ClassificationBadge` in upload preview with inline Household/Personal badge using `bg-primary-muted`/`bg-muted/50`. Replaced `typeBreakdown()` to count household vs personal directly. Column header "Type" → "Scope".
    - Files: `web/src/pages/UploadPage.tsx`

- [x] **UI label renames**
    - Effort: S
    - Status: Completed (2026-04-01)
    - What: "shared spending" → "household spending" in BudgetPage, InsightsPage. "shared budget" → "household budget" in TagReferenceGuide. `total_shared_spending` → `total_household_spending`, `shared_portion` → `household_portion` in DashboardPage. Orval codegen regenerated.
    - Files: `web/src/pages/DashboardPage.tsx`, `web/src/pages/BudgetPage.tsx`, `web/src/pages/InsightsPage.tsx`, `web/src/components/TagReferenceGuide.tsx`

#### Kept as-is (Monarch tag context)

- `SharedTags.TAGS = {"shared", "split"}` in `src/domain/constants.py`
- CSV parser `has_shared` variable and tag-matching logic
- Tag reference guide examples showing `shared` tag syntax
- Test CSV data with `tags: ["shared"]`
- `test_case_insensitive_shared_tag` test name

---

### v1.2.2: Settlement Transaction Linking
**Goal**: When recording a settlement on the Settle Up page, surface matching bank transactions from both partners' CSVs and link them — so they stop counting as spending. Settlement merchants (Venmo, Zelle, etc.) are user-configurable in Settings.
**Status**: Completed (2026-04-03)
**Context**: Concrete example: Bob pays $4000 rent to the landlord (tagged `shared`, 50/50 split). Alice owes Bob $2000. Alice sends $2000 via Venmo to Bob. Both upload CSVs. The system now has three transactions: Bob's -$4000 rent, Alice's -$2000 Venmo expense, and Bob's +$2000 Venmo income. Settlement math is correct (Alice owes $2000), but Alice's personal spending is inflated by $2000 because the Venmo counts as real spending. The Venmo transactions need to be recognized as settlement payments and excluded. The backend already supported `linked_transaction_ids` on `RecordSettlementCommand`, `SettlementTransactionLink` entities, and `mark_transaction_as_settlement` — the frontend never used any of it.
**What this unlocks**: Settlement payments no longer pollute personal spending or budget totals. The Settle Up page becomes the complete money trail — the recorded settlement AND the bank transactions that represent it.

#### Backend

- [x] **Bug fix: Exclude `is_settlement` from budget and reconciliation**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: `_is_budget_relevant()` in `budget.py` and `reconcile()` in `reconciliation.py` only checked `is_excluded`, not `is_settlement`. Added `or tx.is_settlement` to both guards, matching the existing correct check in `_is_personal_budget_relevant()`.
    - Files: `src/domain/budget.py`, `src/domain/reconciliation.py`

- [x] **Configurable settlement merchants**
    - Effort: M
    - Status: Completed (2026-04-03)
    - What: New `SettlementMerchant` entity (`id`, `name`, `merchant_pattern`) with min_length=2 validation. New table via Alembic 0005. Seeded with Venmo, Zelle, Cash App on startup. CRUD via `GET/POST/DELETE /settings/settlement-merchants`. Replaced `SettlementMethod` enum with free-text `str` on `Settlement.method`. Data migration capitalizes existing values.
    - Files: `src/domain/entities/settlement_merchant.py`, `src/domain/repositories/settlement_merchant_repository.py`, `src/infrastructure/persistence/models/settlement_merchant_model.py`, `src/infrastructure/persistence/repositories/settlement_merchant_repository.py`, `src/domain/entities/settlement.py`, `src/interface/api/routes/settings.py`, `src/interface/api/schemas/settings.py`, `alembic/versions/0005_settlement_merchants_and_method.py`

- [x] **Settlement candidate matching**
    - Effort: M
    - Status: Completed (2026-04-03)
    - What: Pure domain function `find_settlement_candidates()` in `settlement_matching.py`. Scores candidates by merchant match (+3), amount proximity (+3), transfer category (+2), personal flag (+1). Disqualifies `is_settlement` or `is_excluded`. Date range: settlement month ±7 days for late-posting tolerance. New `get_by_date_range` on transaction repository. `GET /settlements/candidates` endpoint.
    - Files: `src/domain/settlement_matching.py`, `src/application/use_cases/find_settlement_candidates.py`, `src/interface/api/routes/settlements.py`

- [x] **Enriched linked transaction data on settlement responses**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: `SettlementResponse` now includes `linked_transactions: list[LinkedTransactionResponse]` with `id`, `date`, `merchant`, `amount`, `payer_person_id`. `enrich_with_links()` batch-fetches transaction entities.
    - Files: `src/application/use_cases/_shared/settlement_records.py`, `src/interface/api/schemas/settlements.py`

- [x] **Unlink transaction from settlement**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: `DELETE /settlements/{settlement_id}/links/{transaction_id}`. Removes link and sets `is_settlement=False` if no remaining links. New repo methods `delete_by_settlement_and_transaction` and `get_by_transaction_id`.
    - Files: `src/application/use_cases/unlink_settlement_transaction.py`, `src/domain/repositories/settlement_transaction_link_repository.py`, `src/infrastructure/persistence/repositories/settlement_transaction_link_repository.py`

- [x] **Document `settlement` tag in domain.md**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: Added `settlement` tag row to tag-to-field mapping table and Tag Conventions section.
    - Files: `docs/domain.md`

#### Frontend

- [x] **Settlement merchants in Settings**
    - Effort: M
    - Status: Completed (2026-04-03)
    - What: `SettlementMerchantsEditor` component with list + add form + delete. New Card section in SettingsPage.
    - Files: `web/src/components/SettlementMerchantsEditor.tsx`, `web/src/pages/SettingsPage.tsx`

- [x] **Link transactions during settlement recording**
    - Effort: L
    - Status: Completed (2026-04-03)
    - What: `CandidateChecklist` component with eager background fetch when amount is filled, checkbox selection, match reasons. Integrated into `RecordPaymentForm`. Method dropdown now driven by database merchants with "Other" freeform fallback.
    - Files: `web/src/components/CandidateChecklist.tsx`, `web/src/pages/SettleUpPage.tsx`

- [x] **Linked transactions in payment history**
    - Effort: M
    - Status: Completed (2026-04-03)
    - What: `LinkedTransactionSubrows` component with left-border accent styling. Shown beneath settlements with links. "Link" button for settlements without links.
    - Files: `web/src/components/LinkedTransactionSubrows.tsx`, `web/src/pages/SettleUpPage.tsx`

- [x] **Post-hoc transaction linking**
    - Effort: M
    - Status: Completed (2026-04-03)
    - What: `PosthocLinkDialog` using `<dialog>` + `useDialogSync`. Shows already-linked transactions with unlink buttons and `CandidateChecklist` for adding new links.
    - Files: `web/src/components/PosthocLinkDialog.tsx`, `web/src/pages/SettleUpPage.tsx`

- [x] **Settlement tag in TagReferenceGuide**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: Added row for `settlement` tag documenting the Monarch-side workflow.
    - Files: `web/src/components/TagReferenceGuide.tsx`

---

### v1.2.3: Brand Identity & Icon Upgrade
**Goal**: Replace the generic Heart logo with a purpose-built CoupleFins mark, upgrade Lucide to v1, and reduce icon stroke weight globally.
**Status**: Completed (2026-04-03)
**Context**: The app used a Lucide `Heart` icon as a placeholder logo. The name "CoupleFins" suggests a visual — two hands angled together forming a pair of fins. The Lucide icon library was on v0.577.0; v1.0 had been released with no breaking changes for our 48 icons. Default Lucide stroke-width (2) felt heavy for the app's warm, calm aesthetic.
**What this unlocks**: A distinctive brand mark that visually represents the app name, a current icon library, and lighter icon strokes throughout.

#### Frontend: CoupleFins Logo

- [x] **CoupleFinsLogo component**
    - Effort: M
    - Status: Completed (2026-04-03)
    - What: Custom SVG component compositing two Lucide Hand icon paths — one rotated 45° CW, the other mirrored — forming a pair of fins. Stroke-based, `viewBox="-8 0 40 24"`, `strokeWidth={1.5}`. Replaces `Heart` import in Sidebar, LoginPage, SetupPage, and SetInitialPasswordPage.
    - Files: `web/src/components/CoupleFinsLogo.tsx`, `web/src/components/Sidebar.tsx`, `web/src/pages/LoginPage.tsx`, `web/src/pages/SetupPage.tsx`, `web/src/pages/SetInitialPasswordPage.tsx`

- [x] **Favicon update**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: Single hand rotated 45° CW on dark background (`#37342f`) with rounded corners, teal stroke (`#2d9f93`), `stroke-width="3"` for visibility at 16px. Square `viewBox="0 0 24 24"`. Cache-busting query param on `<link>` in `index.html`.
    - Files: `web/public/favicon.svg`, `web/index.html`

#### Frontend: Lucide v1 Upgrade

- [x] **Upgrade lucide-react 0.577.0 → 1.7.0**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: `pnpm add lucide-react@latest`. All 48 icons compatible, zero breaking changes. All checks pass (lint, types, build, 234 tests).
    - Files: `web/package.json`, `web/pnpm-lock.yaml`

- [x] **Global stroke-width reduction**
    - Effort: S
    - Status: Completed (2026-04-03)
    - What: Added `.lucide { stroke-width: 1.5; }` rule in `@layer base` to reduce all Lucide icons from default 2 to 1.5. Lighter weight matches the app's calm aesthetic.
    - Files: `web/src/app.css`

---

### v1.2.4: Transaction-First Settlement Flow
**Goal**: Replace the manual "Record Payment" form with a transaction-linking flow. Settlements are existing bank transactions you identify, not payments you record.
**Status**: Completed (2026-04-04)
**Context**: The old flow asked users to enter amount, method, and notes — then optionally link bank transactions. This was backwards. The real workflow is: see the balance → pay via Venmo/Zelle (outside app) → come back and mark the matching debit + credit transactions as the settlement.
**What this unlocks**: Simpler, more accurate settlements. No manual data entry. The transactions ARE the evidence.

#### Frontend: Settle Up Redesign

- [x] **Transaction-first settlement flow**
    - Effort: M
    - Status: Completed (2026-04-04)
    - What: Deleted `RecordPaymentForm` (~150 lines). Replaced with `LinkSettlementSection` that promotes `CandidateChecklist` to primary action. Users select a matching transaction pair and click "Mark as settlement" — amount and method derived automatically from the selected transactions. No amount input, no method dropdown, no notes field.
    - Files: `web/src/pages/SettleUpPage.tsx`, `web/src/components/CandidateChecklist.tsx`

- [x] **Candidate month paging**
    - Effort: M
    - Status: Completed (2026-04-04)
    - What: Compact `← Month Year →` nav in the candidate checklist. Searches the selected month + 7 days forward padding. Floor = settlement month, ceiling = latest uploaded transaction month. Backend accepts optional `search_year`/`search_month` query params.
    - Files: `web/src/components/CandidateChecklist.tsx`, `src/application/use_cases/find_settlement_candidates.py`, `src/interface/api/routes/settlements.py`

- [x] **Paired-first candidate display**
    - Effort: S
    - Status: Completed (2026-04-04)
    - What: Candidates sorted by absolute amount (highest first). Transactions with matching amounts shown by default; unmatched behind "Show more" toggle. Max 2 selection with disabled state on unchecked rows.
    - Files: `web/src/components/CandidateChecklist.tsx`

#### Backend: Settlement Delete Fix

- [x] **Reset `is_settlement` on linked transactions when settlement deleted**
    - Effort: S
    - Status: Completed (2026-04-04)
    - What: `DeleteSettlementUseCase` now fetches linked transactions and resets `is_settlement=False` before deleting links. Previously, orphaned transactions remained flagged and were invisible to candidate search.
    - Files: `src/application/use_cases/delete_settlement.py`

#### Docs

- [x] **User flows updated for transaction-first settlement**
    - Effort: S
    - Status: Completed (2026-04-04)
    - What: Rewrote US-SETTLE-1 (link transactions as settlement), updated US-SETTLE-3 (post-hoc linking), US-SETTLE-4 (history). Updated monthly ritual, IA table, Journey 3 step 4.
    - Files: `docs/user-flows.md`
