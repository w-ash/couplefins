# v1.0.x: PostgreSQL Migration

For strategic overview, see the [planning overview](README.md).

---

### v1.0.0: PostgreSQL Migration
**Goal**: Migrate from SQLite to PostgreSQL 18 on Neon — swap the driver, migrate the schema and data, and take advantage of JSONB+GIN for tag storage.
**Status**: Completed (2026-03-27)
**Context**: The app runs locally on each person's laptop and connects to Neon over the network. Neon handles hosting, connection pooling, backups (point-in-time recovery), and branching. No self-hosted infrastructure needed. SQLAlchemy 2.0+ handles most dialect differences, but SQLite-specific raw SQL (e.g., `json_each()`) needs PostgreSQL equivalents.
**What this unlocks**: Multi-device access (check settlement from your phone), JSONB+GIN for O(log n) tag queries, concurrent access without SQLite's write lock, and a foundation for server-side tag filtering.

#### Database Infrastructure

- [x] **PostgreSQL connection layer**
    - Effort: M
    - Dependencies: v0.11.3
    - Status: Completed (2026-03-27)
    - What: Replaced `sqlite+aiosqlite` with `postgresql+asyncpg`. Added `.env` file loading via pydantic-settings. `DatabaseConfig.sync_url` property for psycopg sync access (Alembic, CLI). `pool_pre_ping=True` for Neon connection resilience.

- [x] **JSONB tag column + GIN index**
    - Effort: S
    - Dependencies: "PostgreSQL connection layer"
    - Status: Completed (2026-03-27)
    - What: `tags_json String` → `tags JSONB` with GIN index. Repository uses native JSONB (no `json.dumps`/`json.loads`). `get_distinct_tags()` uses `jsonb_array_elements_text` — PostgreSQL-native, no Python-side parsing.

#### Cleanup

- [x] **Remove SQLite entirely**
    - Effort: S
    - Dependencies: "PostgreSQL connection layer"
    - Status: Completed (2026-03-27)
    - What: Removed `aiosqlite` dependency, all SQLite code paths, directory creation logic. Added `asyncpg` + `psycopg[binary]`.

- [x] **Alembic migration setup**
    - Effort: S
    - Dependencies: "PostgreSQL connection layer"
    - Status: Completed (2026-03-27)
    - What: Deleted 12 SQLite-era migrations. Fresh PostgreSQL baseline migration (`0001_initial_postgresql_schema.py`). Simplified `env.py` to sync psycopg (no async needed for migrations).

- [x] **Test database strategy**
    - Effort: M
    - Dependencies: "PostgreSQL connection layer"
    - Status: Completed (2026-03-27)
    - What: Integration tests use `DATABASE__URL` env var (Neon branch or local PostgreSQL). `TRUNCATE CASCADE` cleanup between tests. Tests skip cleanly without a DB URL. Shared `_truncate_all` helper.

- [x] **Update docs and essential commands**
    - Effort: S
    - Dependencies: "Remove SQLite entirely"
    - Status: Completed (2026-03-27)
    - What: Updated CLAUDE.md, `.env.example`, backlog, and key technical decisions. Database setup instructions cover both Neon and local PostgreSQL.

---

### v1.0.1: Multi-User Readiness
**Goal**: Harden the app for two people using it simultaneously against the same Neon database, and optimize indexes for PostgreSQL.
**Status**: Completed (2026-03-28)
**Context**: With both partners connecting to Neon from separate laptops, the together session is the critical concurrent window. Data freshness and index coverage need attention before real multi-user usage. Neon LISTEN/NOTIFY is a poor fit (incompatible with connection pooler, killed by scale-to-zero, no message guarantees). Smart polling is proportional; SSE is the upgrade path if needed.
**What this unlocks**: Confident two-person usage — both partners see each other's changes within seconds, and queries are properly indexed for PostgreSQL.

#### Multi-User Hardening

- [x] **Smart polling for together-session pages**
    - Effort: S
    - Dependencies: v1.0.0
    - Status: Completed (2026-03-28)
    - What: Added `refetchInterval: 5_000` to TanStack Query hooks on Dashboard, Settle Up, and Transactions pages. Audited existing `staleTime` (30s) and `refetchOnWindowFocus` (true) — both complementary, no changes needed. Scoped to together-session pages only (not Upload/Settings/Insights).
    - Why: The together session is the one time both people use the app simultaneously. Without polling, one person records a payment or locks the month and the other sees stale data until manual refresh.

- [x] **SSE event bus for instant cross-user sync**
    - Effort: M
    - Dependencies: "Smart polling for together-session pages"
    - Status: Completed (2026-03-28)
    - What: Added `GET /api/v1/events` SSE endpoint using FastAPI's built-in `EventSourceResponse` + `ServerSentEvent` with an in-memory `asyncio.Queue` per connected client. After any mutation (upload, settlement, finalization, transaction edit), broadcasts the affected entity name (e.g., `{"entity": "settlements"}`). Frontend `useRealtimeSync` hook opens an `EventSource` connection and calls `queryClient.invalidateQueries()` on received events. `EventSource` provides auto-reconnection for free. Works with existing httpOnly cookie auth. Smart polling remains as fallback.
    - Why: Upgrades from 5-second polling to sub-second updates. The in-memory event bus is proportional — no Redis, no message broker, no persistent connections to Neon. If one user is offline when an event fires, they get fresh data on next page load via normal TanStack Query mechanics.
    - Notes:
        - Skip WebSockets — no bidirectional communication needed, SSE is simpler and auto-reconnects
        - Skip Neon LISTEN/NOTIFY — incompatible with connection pooling, killed by scale-to-zero, no delivery guarantees
        - In-memory queue means events are lost on server restart — acceptable for a local dev server with 2 users

#### Index Optimization

- [x] **PostgreSQL index audit**
    - Effort: S
    - Dependencies: v1.0.0
    - Status: Completed (2026-03-28)
    - What: Audited all indexes against query patterns. Added two high-impact indexes via Alembic migration 0002: (1) partial index `ix_transactions_non_settlement_date` on `(household, date) WHERE NOT is_settlement` — covers 4 repository methods that filter non-settlement transactions; (2) `ix_settlements_year_month` on `(year, month)` — covers period lookups. Skipped low-cardinality tables (persons, categories, reconciliation_periods, budgets).
    - Why: Indexes are the highest-leverage, lowest-risk performance improvement. A systematic audit prevents ad-hoc index additions.

---

### v1.0.2: Query Optimization
**Goal**: Ship PostgreSQL-native query improvements and harden connection handling for Neon's PgBouncer pooler.
**Status**: Completed (2026-03-28)
**Context**: v1.0.0 established the PostgreSQL foundation and v1.0.1 shipped multi-user readiness. This version exploits PostgreSQL capabilities for tag queries and server-side filtering, batches sequential round-trips to reduce Neon latency, and fixes a connection pool correctness issue with PgBouncer.
**What this unlocks**: Server-side tag filtering via GIN index, fewer round-trips per request, and reliable connection handling through Neon's pooled endpoint.

#### Connection Handling

- [x] **Neon connection pool tuning**
    - Effort: S
    - Dependencies: v1.0.1
    - Status: Completed (2026-03-28)
    - What: Auto-detect Neon's `-pooler` endpoint in the database URL. Pooled endpoint: `NullPool` + `statement_cache_size=0` (asyncpg's prepared statement cache conflicts with PgBouncer's transaction-mode pooling). Direct endpoint: `QueuePool` with `pool_size=3, max_overflow=2` (proportional for 2 users). `pool_pre_ping=True` in both paths for Neon scale-to-zero resilience.
    - Why: Without `statement_cache_size=0`, asyncpg caches named prepared statements that PgBouncer can route to a different backend PostgreSQL process, causing intermittent `prepared statement does not exist` errors.

#### Tag Queries

- [x] **`get_distinct_tags()` via JSONB**
    - Effort: S
    - Dependencies: v1.0.0
    - Status: Completed (2026-03-27) — done as part of v1.0.0
    - What: Replaced Python-side `json.loads()` loop with `SELECT DISTINCT jsonb_array_elements_text(tags)`.

- [x] **Server-side tag filtering**
    - Effort: M
    - Dependencies: "`get_distinct_tags()` via JSONB"
    - Status: Completed (2026-03-28)
    - What: Added optional `tags` query parameter to `GET /reconciliation`. SQL filter: `WHERE tags @> cast(:tags as jsonb)` using the GIN index. Threaded through all layers: domain Protocol (`tags: tuple[str, ...] | None` keyword param on three repo methods), infrastructure (`@>` operator via `_tag_filter` helper), use case (`GetReconciliationCommand.tags`), and API route. Frontend unchanged — client-side `useTransactionFilters` stays for instant interaction; server-side filtering available for deep-linking.
    - Why: Reduces payload size when filtering by tag. Foundation for future tag-based views.

#### Query Patterns

- [x] **Sequential query batching**
    - Effort: S
    - Dependencies: v1.0.0
    - Status: Completed (2026-03-28)
    - What: Batched three sequential query patterns: (1) `validate_settlement_persons()` — two `get_by_id()` → single `get_by_ids()`. (2) `assert_periods_not_finalized()` — N sequential `get_by_period()` → single `get_by_periods()` (new repo method using `OR` of `AND` conditions). (3) `bulk_update_mappings()` — N sequential `get_by_id()` → single `get_by_ids()`. Added `get_by_ids` to Person and CategoryGroup Protocols, `get_by_periods` to ReconciliationPeriod Protocol. Skipped cross-repo pairs (`mark_transaction_as_settlement`, `save_budget`) — different entity types can't be batched with `get_by_ids()`.
    - Why: Each round-trip to Neon has ~10-40ms network latency. Batching eliminates unnecessary sequential queries on the critical settlement and editing paths.

---

### v1.0.3: DRY Audit, Theme Preference & Auth UX
**Goal**: Address DDD/DRY audit findings, move theme preference to the database, fix Neon sslmode connection issue, and improve password setup UX.
**Status**: Completed (2026-03-28)
**Context**: A full architecture audit identified 13 duplicated `get_by_id` + `NotFoundError` patterns, 3-way duplicated payer_percentage validation, and theme preference stored only in localStorage (breaks on DB reset). The auth flow needed confirm-password fields and show/hide toggles.
**What this unlocks**: Cleaner codebase with less duplication, per-person theme preference that survives device changes, and a more trustworthy password setup experience.

#### DRY Improvements

- [x] **`require_by_id()` helper**
    - Effort: S
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: New generic helper in `_shared/entity_lookup.py`. Replaced 13 identical `get_by_id` + `if None: raise NotFoundError` blocks across use cases (`update_person`, `delete_budget`, `save_budget`, `upload_csv`, `preview_csv`, `delete_category_group`, `update_category_group`, `update_budget`, `get_transaction_edits`, `mark_transaction_as_settlement`, `delete_settlement`, `change_password`).

- [x] **Single-source payer_percentage validation**
    - Effort: S
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: Added `check_payer_percentage()` to `domain/splits.py`. Entity `__attrs_post_init__` delegates to it. Application `validate_payer_percentage()` wraps it (ValueError → ValidationError). Schema validators call it directly. Eliminated 3-way duplication of the bounds check.

- [x] **Date helper consolidation**
    - Effort: S
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: Added `date_year_prefix()` to `base.py` alongside `date_month_prefix`. Moved `month_bounds()` to `domain/date_math.py` (pure domain logic). Moved `reconcile_all_months()` from application `_shared/reconciliation_helpers.py` to `domain/reconciliation.py`. Removed the now-empty re-export module.

#### Theme Preference

- [x] **DB-backed theme preference**
    - Effort: M
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: Added `theme_preference` field to Person entity/model (Alembic migration 0003, `server_default="system"`). Included in `PersonResponse` and `UpdatePersonRequest`. Before login: FOIT script uses system `prefers-color-scheme` (no stale localStorage override). After login: `/auth/me` returns `theme_preference`, ThemeProvider applies it and caches in localStorage. Settings toggle saves to DB via `PATCH /persons/{id}`. On logout: localStorage cleared, reverts to system. Auth pages (Setup, Login, SetInitialPassword) have a ThemeToggle for manual switching before login.

#### Connection Fix

- [x] **asyncpg sslmode compatibility**
    - Effort: S
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: Neon URLs include `?sslmode=require` which asyncpg doesn't accept as a URL parameter. Added `async_url` property to `DatabaseConfig` that strips `sslmode` from the URL, and `async_connect_args` that translates it to asyncpg's `ssl="require"` connect arg. Engine creation uses both.
    - Why: Server crashed on startup with `TypeError: connect() got an unexpected keyword argument 'sslmode'`.

#### Auth UX

- [x] **Password visibility toggle + confirm fields**
    - Effort: S
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: New `PasswordInput` component with show/hide toggle (Eye/EyeOff icons, `aria-pressed`, `aria-controls`, 40px touch target). SetupPage now has confirm-password for each person — submit blocked until both match. SetInitialPasswordPage and LoginPage also use the new component. Follows WCAG accessible toggle pattern.

#### Cleanup

- [x] **Vulture whitelist + CSS warning**
    - Effort: S
    - Dependencies: v1.0.2
    - Status: Completed (2026-03-28)
    - What: Whitelisted `has_password` (Pydantic field serialized by FastAPI) and `compute_trailing_average` (tested domain function, planned for future Insights use). Removed `!important` from `prefers-reduced-motion` CSS by using `animation-duration: 0s` instead. Fixed Orval codegen failure by stripping SSE endpoint from exported OpenAPI spec.
