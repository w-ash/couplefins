# v1.8.2 — Chat Write Parity

Status: Planned
Effort: L
Depends on: v1.8.1 (read tools — several mutations reference IDs surfaced by them)
Series overview: [v1.8.x — Agentic Chat](v1.8.x-agentic-chat.md)

## Goal

Every mutation a human can perform through the app becomes a chat tool, all
using the existing two-phase propose/confirm pattern. After this version,
`PENDING_PARITY_USE_CASES` in `src/application/chat/registry.py` is empty and
can be deleted (update the parity test accordingly).

## Context for a fresh agent

- **The two-phase pattern is non-negotiable** (prompt-injection defense: a
  mutation is a proposal until the human clicks Confirm). Flow:
  1. Propose handler in `src/application/chat/tool_executor.py`: validate
     (finalization check via `_check_finalization` for period-scoped writes,
     entity-exists checks, name→ID resolution), then `_propose_action(...)` →
     returns `{"status": "pending_confirmation", action_id, description, details}`.
  2. The frontend renders a ConfirmationCard; the user's next POST carries
     `confirmation: {action_id, approved}`.
  3. Executor in `src/application/chat/confirmed_actions.py` with signature
     `(action: PendingAction, current_user: Person) -> dict[str, object]`:
     atomic UoW, TOCTOU re-checks (re-verify the entity still exists /
     period still unlocked), single commit.
  4. Wire both into a `ToolSpec(kind="write", executor=..., broadcast_entity=...)`
     in `registry.py`. Broadcast entities must be valid `BroadcastEntity`
     literals (`src/infrastructure/events/event_bus.py`): settlements,
     transactions, uploads, reconciliation, budgets.
- Follow `update_budget` (propose: `handle_update_budget`; execute:
  `exec_budget`) end-to-end as the template.
- Descriptions lead with a trigger sentence AND state that the tool proposes
  (never applies) the change. Schemas set `strict: true` (house convention as
  of v1.8.1).
- **Confirmation cards must render concrete before/after details, never just
  an action name.** Anthropic's containment write-up (May 2026) measured ~93%
  of permission prompts being rubber-stamped — the details payload is what
  makes human confirmation a real defense rather than approval theater. The
  propose handler owns building those details (current amounts being deleted,
  counts being copied, where categories move, etc. — the Notes column below).
- Write tools never get `allowed_callers` — mutations stay main-loop
  proposals only. The v1.8.3 code-execution sandbox may call read tools
  programmatically but must never be able to invoke a write, even as a
  proposal.
- The registry shape test enforces executor + broadcast_entity on every
  write ToolSpec.

## Tools to add (12 tools ← 17 pending use cases)

| Tool | Inputs | Use case(s) | Notes |
|---|---|---|---|
| `delete_budget` | `group_name`, `year`, `month`, `scope?` | `DeleteBudgetUseCase` | Resolve group name → budget id at propose time; details show current amount being deleted. |
| `copy_budgets` | `from_year`, `from_month`, `to_year`, `to_month`, `scope?` | `CopyBudgetsUseCase` | Details list how many budgets will be copied (no overwrites — mirror the UI semantics). |
| `manage_category_group` | `action: create\|rename\|delete`, `name`, `new_name?`, `move_categories_to?` | `CreateCategoryGroupUseCase`, `UpdateCategoryGroupUseCase`, `DeleteCategoryGroupUseCase` | One tool, action enum — the confirmation description carries the action. Delete must surface where categories move. |
| `map_categories` | `mappings: [{category, group_name}]` | `BulkUpdateMappingsUseCase` | Resolve group names → ids at propose time; reject unknown groups with an actionable error listing valid names. |
| `set_category_personal` | `category`, `include_personal: bool` | `UpdateCategoryUseCase` | |
| `finalize_period` | `year`, `month` | `FinalizePeriodUseCase` | Surface `finalization_warnings` (from GetSettleUpData) in the proposal details — advisory, not blocking, matching the UI. |
| `unfinalize_period` | `year`, `month` | `UnfinalizePeriodUseCase` | Separate tool — opposite-direction actions never share a schema. |
| `record_settlement` | `from_person`, `to_person`, `amount`, `method?`, `notes?`, `year?`, `month?` | `RecordSettlementUseCase` | year/month = display-only "recorded against" annotation (ledger applies FIFO). Person names → ids. Wrap notes in `<user_data>` when echoed. |
| `waive_settlement` | `notes?` | `RecordWaivedSettlementUseCase` | Waives the outstanding balance (all-months, per the v1.7.5 ledger). |
| `delete_settlement` | `settlement_id` | `DeleteSettlementUseCase` | ID from `get_settlement_activity` (v1.8.1). Unlinks transactions; works regardless of month locks (US-SETTLE-4). |
| `link_settlement_transaction` / `unlink_settlement_transaction` | `transaction_id` (+ `settlement_id` for unlink) | `MarkTransactionAsSettlementUseCase`, `UnlinkSettlementTransactionUseCase` | Two tools (opposite directions). IDs from `get_settlement_activity` candidates. |
| `manage_settlement_merchant` | `action: add\|remove`, `name`, `pattern?` | `CreateSettlementMerchantUseCase`, `DeleteSettlementMerchantUseCase` | Action enum. |

Also extend the existing `update_transaction_split` schema to accept
`splits: [{transaction_id, payer_percentage}]` (batch — human parity with
`PATCH /transactions/splits`); keep the single-entry form working. The
executor already uses `UpdateTransactionSplitsUseCase`, which is batch-first.

## Registry bookkeeping

- Move each use case from `PENDING_PARITY_USE_CASES` into its ToolSpec's
  `use_cases`. When the set is empty, delete it and drop it from the parity
  test's classification union.
- `UpdateBudgetUseCase` (PUT /budgets/{id}) is covered by the existing
  `update_budget` tool's upsert semantics via `SaveBudgetUseCase` — add
  `"UpdateBudgetUseCase"` to that ToolSpec's `use_cases` tuple rather than
  building a separate tool (capability parity, not endpoint parity).

## Frontend

- `web/src/components/chat/ConfirmationCard.tsx`: add a generic details
  renderer (key/value list from `details`) as the `DetailDisplay` default
  branch; keep the three bespoke renderers.
- `web/src/components/chat/ToolCallIndicator.tsx`: add labels; extend
  `MUTATION_TOOLS` (or better: derive it — anything not in a read set —
  and add a vitest cross-checking it against the tool names the backend
  streams).

## Tests

- Propose-handler unit tests: validation failures, finalized-period
  rejection, unknown-entity errors (actionable messages), happy paths →
  `pending_confirmation` shape.
- Executor unit tests in `tests/unit/application/chat/test_confirmed_actions.py`
  pattern: happy path, TOCTOU (target deleted between propose and confirm),
  atomicity where multiple writes share a UoW.
- One end-to-end confirm flow integration test for a representative new
  mutation (suggest `finalize_period`): propose via fake LLM → POST with
  confirmation → assert period locked + broadcast.
- Parity test green with PENDING empty.

## Docs

- `docs/user-flows.md`: **US-CHAT-18** (manage budgets & categories via chat),
  **US-CHAT-19** (settle up and finalize via chat with confirmation) — include
  the blacklist boundary as an explicit criterion ("Given I ask the assistant
  to change my password/theme, then it declines and points me to the UI").
- Backlog README: flip this row to Completed.

## Acceptance

- Given any mutation available in the UI (except auth/setup/preferences/CSV
  upload), when the user asks for it in chat, then the assistant proposes it
  with a confirmation card, and confirming applies it identically to the UI
  path (audit trail, SSE broadcast, finalization guards).
- Full quality gate green (backend + frontend).
